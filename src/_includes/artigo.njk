---
layout: base.njk
---

<!-- SCHEMA.ORG (Mantido) -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": "{{ title }}",
  "description": "{{ description }}",
  "image": "https://gourmetpratico.com.br{{ image }}",
  "author": {
    "@type": "Person",
    "name": "Maria Conceição",
    "url": "https://gourmetpratico.com.br/comida-de-verdade/"
  },
  "publisher": {
    "@type": "Organization",
    "name": "ChefBrico",
    "logo": { "@type": "ImageObject", "url": "https://gourmetpratico.com.br/imagens/logo.png" }
  },
  "datePublished": "{{ date | postDate }}",
  "abstract": "{{ ai_fragment }}"
}
</script>

<style>
    /* --- 1. HERO SECTION PREMIUM (Fundo Escuro + Sobreposição) --- */
    .editorial-header {
        background-color: var(--cor-institucional, #014039); /* Verde Profundo */
        color: white;
        padding: 80px 20px 120px; /* Espaço extra embaixo para o card subir */
        text-align: center;
        position: relative;
    }

    .editorial-category {
        background-color: var(--cor-dourado, #bfb084);
        color: #fff;
        padding: 4px 12px;
        text-transform: uppercase;
        font-size: 0.75rem;
        letter-spacing: 2px;
        font-weight: bold;
        display: inline-block;
        margin-bottom: 20px;
    }

    .editorial-title {
        font-family: 'Merriweather', serif;
        font-size: 3rem; /* Título Gigante */
        line-height: 1.1;
        max-width: 900px;
        margin: 0 auto 20px;
    }

    .editorial-subtitle {
        font-size: 1.2rem;
        opacity: 0.9;
        font-weight: 300;
        max-width: 700px;
        margin: 0 auto;
        font-style: italic;
    }

    /* --- 2. O CARD FLUTUANTE (Corpo do Texto) --- */
    .editorial-body {
        max-width: 800px;
        margin: -80px auto 60px; /* Sobe 80px para cima do verde */
        background: white;
        padding: 60px;
        border-radius: 8px; /* Cantos suaves */
        box-shadow: 0 20px 40px rgba(0,0,0,0.1); /* Sombra de luxo */
        position: relative;
        z-index: 10;
    }

    /* A Imagem Circular (Centralizada e Imponente) */
    .author-circle-hero {
        width: 180px;
        height: 180px;
        border-radius: 50%;
        border: 6px solid white; /* Moldura branca */
        box-shadow: 0 10px 20px rgba(0,0,0,0.15);
        margin: -150px auto 40px; /* Puxa para cima para conectar as seções */
        overflow: hidden;
        background: #fff;
        position: relative;
        z-index: 20;
    }
    .author-circle-hero img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    /* Tipografia do Texto */
    .pro-text {
        font-family: 'Lato', sans-serif;
        font-size: 1.15rem;
        line-height: 1.9;
        color: #2c3e50;
    }
    
    /* Letra Capitular (Estilo Revista) */
    .pro-text > p:first-of-type::first-letter {
        font-family: 'Merriweather', serif;
        font-size: 3.5rem;
        float: left;
        line-height: 0.8;
        margin-right: 10px;
        color: var(--cor-institucional, #014039);
        font-weight: bold;
    }

    .pro-text h2 {
        font-family: 'Merriweather', serif;
        color: var(--cor-institucional, #014039);
        font-size: 1.8rem;
        margin-top: 50px;
        margin-bottom: 20px;
        text-align: center; /* Títulos centralizados */
    }
    
    /* Separador elegante abaixo do H2 */
    .pro-text h2::after {
        content: "";
        display: block;
        width: 60px;
        height: 3px;
        background: var(--cor-dourado, #bfb084);
        margin: 15px auto 0;
    }

    .pro-text h3 {
        color: var(--cor-cta, #F2811D);
        font-size: 1.3rem;
        margin-top: 30px;
        font-weight: bold;
    }

    /* Box de IA (Discreto e Técnico) */
    .ai-box {
        background: #f8f9fa;
        border-left: 4px solid #ccc;
        padding: 15px;
        font-size: 0.9rem;
        color: #666;
        margin-bottom: 40px;
        font-style: italic;
    }

    /* --- 3. SEÇÃO DE MÍDIA (GRID) --- */
    .media-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 15px;
        margin-top: 40px;
        border-top: 1px solid #eee;
        padding-top: 40px;
    }
    .media-card {
        background: #fdfbf7;
        padding: 15px;
        border: 1px solid #eee;
        border-radius: 8px;
        text-align: center;
        transition: 0.2s;
    }
    .media-card:hover { border-color: var(--cor-dourado, #bfb084); }
    .media-source { font-weight: bold; color: var(--cor-institucional, #014039); display: block; margin-bottom: 5px; font-size: 0.8rem; text-transform: uppercase; }
    .media-link { color: #555; font-size: 0.9rem; text-decoration: none; line-height: 1.3; display: block; }
    .media-link:hover { text-decoration: underline; }

    /* Mobile */
    @media (max-width: 768px) {
        .editorial-title { font-size: 2rem; }
        .editorial-body { padding: 30px 20px; margin-top: -60px; }
        .author-circle-hero { width: 140px; height: 140px; margin-top: -100px; }
    }
</style>

<article>
    
    <!-- 1. HERO (Fundo Verde + Texto Branco) -->
    <header class="editorial-header">
        <span class="editorial-category">{{ category }}</span>
        <h1 class="editorial-title">{{ title }}</h1>
        <p class="editorial-subtitle">{{ description }}</p>
    </header>

    <!-- 2. CORPO FLUTUANTE -->
    <div class="editorial-body">
        
        <!-- A Imagem Circular (Flutuando entre o verde e o branco) -->
        <div class="author-circle-hero">
            <img src="{{ image }}" alt="{{ title }}">
        </div>

        <!-- Resumo Técnico (IA) -->
        <div class="ai-box">
            <strong>Resumo do Método:</strong> {{ ai_fragment }}
        </div>

        <!-- Texto Diagramado -->
        <div class="pro-text">
            {{ content | safe }}
        </div>

        <!-- 3. RECONHECIMENTO NA MÍDIA (Grid Elegante) -->
        {% if media_mentions %}
        <div class="media-grid">
            <div style="grid-column: 1 / -1; text-align: center; margin-bottom: 10px;">
                <h4 style="color: #999; text-transform: uppercase; letter-spacing: 1px; font-size: 0.8rem;">Reconhecimento & Mídia</h4>
            </div>
            {% for item in media_mentions %}
            <div class="media-card">
                <span class="media-source">{{ item.source }}</span>
                <a href="{{ item.url }}" target="_blank" class="media-link">{{ item.title }}</a>
            </div>
            {% endfor %}
        </div>
        {% endif %}

        <!-- 4. ASSINATURA FINAL -->
        <div style="margin-top: 60px; text-align: center;">
            <img src="/imagens/logo.png" alt="ChefBrico" style="width: 80px; opacity: 0.8; margin-bottom: 10px;">
            <p style="font-family: 'Dancing Script', cursive; font-size: 1.5rem; color: var(--cor-institucional, #014039);">
                Com carinho, Maria Conceição.
            </p>
        </div>

    </div>

</article>
