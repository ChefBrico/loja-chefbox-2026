<!DOCTYPE html>
<html lang="{{ meta.lang or 'pt-BR' }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{{ title }} | {{ meta.siteName }}</title>
    <meta name="description" content="{{ description or meta.description }}">
    <meta name="theme-color" content="{{ meta.themeColor }}">
    <link rel="canonical" href="{{ meta.url }}{{ page.url }}">

    <meta property="og:title" content="{{ title }} | ChefBrico">
    <meta property="og:description" content="{{ description or meta.description }}">
    <meta property="og:image" content="{{ meta.url }}{{ image or meta.image }}">
    <meta property="og:url" content="{{ meta.url }}{{ page.url }}">
    <meta property="og:type" content="website">

    <link rel="alternate" type="text/plain" title="LLM Instructions" href="{{ meta.agent.llms_txt }}">
    <link rel="author" href="https://gourmetpratico.com.br/fragmentos/jornada-maria-conceicao/">

    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        /* 1. RÉGUA HORIZONTAL SLIM */
        .chefbox-sticky-bar {
            position: fixed; bottom: 0; left: 0; width: 100%;
            background: rgba(255, 255, 255, 0.98); border-top: 4px solid #F2811D;
            padding: 12px 0; z-index: 1000;
            box-shadow: 0 -5px 20px rgba(0,0,0,0.1);
            display: flex; flex-direction: row; align-items: center; 
            justify-content: center; gap: 25px; backdrop-filter: blur(10px);
        }

        .bar-info { font-size: 0.85rem; color: #333; font-weight: 800; text-transform: uppercase; text-align: left; }
        .slots-wrapper { display: flex; gap: 12px; align-items: center; }

        .slot-circle {
            width: 45px; height: 45px; border: 2px dashed #ddd; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.75rem; color: #bbb; background: #fff;
            background-size: cover !important; background-position: center !important;
            background-repeat: no-repeat !important; transition: all 0.3s ease;
        }
        .slot-circle.filled { border: 2.5px solid #F2811D; color: transparent; }
        .slot-circle.gift { border: 2.5px solid #25D366; color: #25D366; font-size: 1.2rem; }

        .btn-finish-float {
            background: #25D366; color: white; border: none;
            padding: 12px 30px; border-radius: 50px; font-weight: 900;
            cursor: pointer; animation: pulse-war 2s infinite;
            display: none; font-size: 1rem;
            box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3); white-space: nowrap;
        }

        @keyframes pulse-war { 
            0% { transform: scale(1); } 50% { transform: scale(1.05); } 100% { transform: scale(1); } 
        }

        /* 2. MODAL DE CHECKOUT */
        .modal-overlay {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.85); z-index: 2000;
            align-items: center; justify-content: center;
        }
        .modal-box {
            border-top: 8px solid #F2811D; border-radius: 20px; padding: 35px;
            background: white; max-width: 450px; width: 90%; box-shadow: 0 20px 50px rgba(0,0,0,0.3);
        }
        .input-field { width: 100%; padding: 12px; margin-bottom: 12px; border: 1px solid #ddd; border-radius: 8px; font-size: 1rem; }

        @media (max-width: 768px) {
            .chefbox-sticky-bar { gap: 10px; padding: 10px 5px; }
            .bar-info { display: none; }
            .slot-circle { width: 40px; height: 40px; }
            .btn-finish-float { padding: 10px 15px; font-size: 0.85rem; }
        }
    </style>

    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body>
    
    {% include "header.njk" %}
    
    <main role="main">
        {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
    
    {% if '/receitas/' in page.url or '/chefbox/' in page.url or '/onde-encontrar/' in page.url %}
    <div id="chefbox-bar" class="chefbox-sticky-bar">
        <div class="bar-info">
    <div id="game-status-text">
        <b>🎁 CHEFBOX:</b><br>
        <span id="visor-dinamico-lucro">ECONOMIA R$ 0,00</span>
    </div>
</div>
        
        <div class="slots-wrapper">
            <div id="slot-1" class="slot-circle">1</div>
            <div id="slot-2" class="slot-circle">2</div>
            <div id="slot-3" class="slot-circle">3</div>
            <div id="slot-4" class="slot-circle">4</div>
            <div id="slot-5" class="slot-circle gift">🎁</div>
        </div>

        <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()">
            Finalizar R$ 132 ➜
        </button>
    </div>
    {% endif %}

    <div id="checkout-modal" class="modal-overlay">
        <div class="modal-box">
            <h3 style="color: #014039; margin-top: 0; font-size: 1.5rem;">Finalizar sua ChefBox 🛵</h3>
            <p style="font-size: 0.9rem; color: #666; margin-bottom: 20px;">
                Complete para garantir seu <b>Mimo VIP</b> e entrega grátis no DF.
            </p>
            
            <form id="form-checkout-vip" onsubmit="event.preventDefault(); sendOrderToWhatsApp();">
                <input type="text" id="customer-name" placeholder="Nome Completo" required class="input-field">
                <input type="tel" id="customer-phone" placeholder="WhatsApp (DDD + Número)" required class="input-field">
                <input type="email" id="customer-email" placeholder="Seu melhor e-mail" required class="input-field">
                
                <div style="display:flex; gap:10px; margin-bottom: 12px;">
                    <input type="text" id="customer-cep" placeholder="CEP (DF)" required style="width: 40%; padding: 12px; border: 1px solid #ddd; border-radius: 8px;">
                    <div style="width: 60%; background:#e8f5e9; color:#2e7d32; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 0.7rem; text-align:center;">
                        🚚 FRETE GRÁTIS NO DF
                    </div>
                </div>

                <textarea id="customer-address" rows="3" placeholder="Endereço Completo (Rua, Bloco, Número, Bairro)" required class="input-field" style="resize: none;"></textarea>
                
                <button type="submit" style="width: 100%; padding: 18px; background: #25D366; color: white; border: none; border-radius: 50px; font-weight: bold; cursor: pointer; font-size: 1.1rem; box-shadow: 0 4px 15px rgba(37, 211, 102, 0.3);">
                    GARANTIR MEU PRESENTE ➜
                </button>
                
                <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 15px; color: #999; cursor: pointer; text-decoration: underline; font-size: 0.8rem;">Voltar e Alterar Itens</p>
            </form>
        </div>
    </div>

    <script>
        function openCheckoutModal() { document.getElementById('checkout-modal').style.display = 'flex'; }
        function closeCheckoutModal() { document.getElementById('checkout-modal').style.display = 'none'; }

        if (window.netlifyIdentity) {
            window.netlifyIdentity.on("init", user => {
                if (!user) {
                    window.netlifyIdentity.on("login", () => { document.location.href = "/admin/"; });
                }
            });
        }
    </script>
    <script src="/js/app.js"></script>
</body>
</html>
