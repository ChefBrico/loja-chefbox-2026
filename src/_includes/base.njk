<!DOCTYPE html>
<html lang="{{ meta.lang or 'pt-BR' }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{{ title }} | {{ meta.siteName }}</title>
    <meta name="description" content="{{ description or meta.description }}">
    <meta name="theme-color" content="#FFFFFF">
    <link rel="canonical" href="{{ meta.url }}{{ page.url }}">

    <meta property="og:title" content="{{ title }} | ChefBrico">
    <meta property="og:description" content="{{ description or meta.description }}">
    <meta property="og:image" content="{{ meta.url }}{{ image or meta.image }}">
    <meta property="og:url" content="{{ meta.url }}{{ page.url }}">
    <meta property="og:type" content="website">

    <link rel="alternate" type="text/plain" title="LLM Instructions" href="{{ meta.agent.llms_txt }}">
    <link rel="author" href="https://gourmetpratico.com.br/comida-de-verdade/">

    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        /* --- INTERFACE GOLD STANDARD (APPLE STYLE) --- */
        :root {
            --chef-orange: #F2811D;
            --chef-green: #014039;
            --whatsapp-green: #25D366;
            --apple-cream: #FDFBF7;
            --apple-white: #FFFFFF;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
            -webkit-font-smoothing: antialiased;
            background-color: var(--apple-white);
            color: #1d1d1f;
        }

        /* 1. RÉGUA CHEFBOX (FLOATING CAPSULE) */
        .chefbox-sticky-bar {
            position: fixed; bottom: 30px; left: 50%; transform: translateX(-50%);
            width: 90%; max-width: 580px;
            background: rgba(255, 255, 255, 0.82);
            backdrop-filter: blur(25px) saturate(180%);
            -webkit-backdrop-filter: blur(25px) saturate(180%);
            border: 1px solid rgba(255, 255, 255, 0.4);
            border-top: 3px solid var(--chef-orange);
            border-radius: 40px;
            padding: 12px 20px; z-index: 10000;
            box-shadow: 0 15px 40px rgba(0,0,0,0.12);
            display: flex; align-items: center; justify-content: space-between;
            gap: 15px;
        }

        .bar-info { display: flex; flex-direction: column; line-height: 1.2; }
        .bar-info b { color: var(--chef-green); font-size: 0.65rem; text-transform: uppercase; letter-spacing: 1.5px; font-weight: 800; }
        #visor-dinamico-lucro { font-size: 0.8rem; font-weight: 900; color: var(--chef-orange); }

        .slots-wrapper { display: flex; gap: 8px; align-items: center; }

        .slot-circle {
            width: 44px; height: 44px; border: 1.5px solid #eee; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.75rem; color: #aaa; background: var(--apple-white);
            background-size: cover !important; background-position: center !important;
            transition: all 0.5s cubic-bezier(0.2, 1, 0.3, 1);
        }
        .slot-circle.filled { border: 2px solid var(--chef-orange); box-shadow: 0 8px 20px rgba(242, 129, 29, 0.2); transform: scale(1.05); }
        .slot-circle.gift { border: 2px solid var(--whatsapp-green); color: var(--whatsapp-green); font-size: 1.2rem; background: var(--apple-cream); }

        .btn-finish-float {
            background: var(--whatsapp-green); color: white; border: none;
            padding: 12px 24px; border-radius: 50px; font-weight: 900;
            cursor: pointer; font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.5px;
            box-shadow: 0 10px 25px rgba(37, 211, 102, 0.3);
            white-space: nowrap; animation: pulse-apple 3s infinite;
        }

        @keyframes pulse-apple { 
            0% { transform: scale(1); } 
            50% { transform: scale(1.04); } 
            100% { transform: scale(1); } 
        }

        /* 2. MODAL DE CHECKOUT (iOS STYLE) */
        .modal-overlay {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(1, 64, 57, 0.75); backdrop-filter: blur(10px); z-index: 20000;
            align-items: center; justify-content: center;
        }
        .modal-box {
            background: var(--apple-white); border-radius: 32px; padding: 45px;
            max-width: 480px; width: 92%; 
            box-shadow: 0 40px 80px rgba(0,0,0,0.3);
            border: 1px solid rgba(255,255,255,0.3);
            text-align: center;
        }
        .input-field { 
            width: 100%; padding: 16px; margin-bottom: 15px; 
            border: 1px solid #f2f2f2; border-radius: 18px; 
            font-size: 1rem; background: #F5F5F7;
            box-sizing: border-box; transition: all 0.3s ease;
        }
        .input-field:focus { border-color: var(--chef-orange); outline: none; background: #fff; box-shadow: 0 0 0 4px rgba(242, 129, 29, 0.1); }

        @media (max-width: 768px) {
            .chefbox-sticky-bar { width: 95%; padding: 10px 15px; bottom: 15px; }
            .bar-info { display: none; }
            .slot-circle { width: 40px; height: 40px; }
            .btn-finish-float { padding: 12px 18px; font-size: 0.75rem; }
        }
    </style>

    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body>
    
    {% include "header.njk" %}
    
    <main role="main">
        {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
    
    {% if '/receitas/' in page.url or '/chefbox/' in page.url or '/onde-encontrar/' in page.url %}
    <div id="chefbox-bar" class="chefbox-sticky-bar">
        <div class="bar-info">
            <div id="game-status-text">
                <b>🎁 CHEFBOX VIP</b>
                <span id="visor-dinamico-lucro">ECONOMIA R$ 0,00</span>
            </div>
        </div>
        
        <div class="slots-wrapper">
            <div id="slot-1" class="slot-circle">1</div>
            <div id="slot-2" class="slot-circle">2</div>
            <div id="slot-3" class="slot-circle">3</div>
            <div id="slot-4" class="slot-circle">4</div>
            <div id="slot-5" class="slot-circle gift">🎁</div>
        </div>

        <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()">
            Finalizar R$ 132 ➜
        </button>
    </div>
    {% endif %}

    <div id="checkout-modal" class="modal-overlay">
        <div class="modal-box">
            <h3 style="color: var(--chef-green); margin-bottom: 12px; font-family: 'Merriweather', serif; font-size: 2rem;">Quase lá! 🛵</h3>
            <p style="font-size: 1rem; color: #515154; margin-bottom: 30px; line-height: 1.5;">
                Preencha os dados para a Maria preparar seu <b>Mimo VIP</b> e agendar sua entrega (D+1) em Brasília.
            </p>
            
            <form id="form-checkout-vip" onsubmit="event.preventDefault(); sendOrderToWhatsApp();">
                <input type="text" id="customer-name" placeholder="Nome Completo" required class="input-field">
                <input type="tel" id="customer-phone" placeholder="WhatsApp (DDD + Número)" required class="input-field">
                <input type="email" id="customer-email" placeholder="E-mail para confirmação" required class="input-field">
                
                <div style="display:flex; gap:12px; margin-bottom: 15px;">
                    <input type="text" id="customer-cep" placeholder="CEP (DF)" required style="width: 42%; padding: 16px; border: 1px solid #f2f2f2; border-radius: 18px; background: #F5F5F7;">
                    <div style="width: 58%; background:#e8f5e9; color:#1b5e20; border-radius: 18px; display: flex; align-items: center; justify-content: center; font-weight: 800; font-size: 0.6rem; text-align:center; text-transform: uppercase; letter-spacing: 1px;">
                        ✅ FRETE GRÁTIS NO DF
                    </div>
                </div>

                <textarea id="customer-address" rows="3" placeholder="Endereço Completo (Rua, Bloco, Número, Bairro)" required class="input-field" style="resize: none;"></textarea>
                
                <button type="submit" style="width: 100%; padding: 22px; background: var(--whatsapp-green); color: white; border: none; border-radius: 60px; font-weight: 900; cursor: pointer; font-size: 1.1rem; box-shadow: 0 12px 30px rgba(37, 211, 102, 0.4); margin-top: 15px; text-transform: uppercase; letter-spacing: 1px;">
                    GARANTIR MEU PRESENTE ➜
                </button>
                
                <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 25px; color: #86868b; cursor: pointer; text-decoration: underline; font-size: 0.85rem; font-weight: 500;">Voltar ao Catálogo</p>
            </form>
        </div>
    </div>

    <script>
        function openCheckoutModal() { document.getElementById('checkout-modal').style.display = 'flex'; }
        function closeCheckoutModal() { document.getElementById('checkout-modal').style.display = 'none'; }

        if (window.netlifyIdentity) {
            window.netlifyIdentity.on("init", user => {
                if (!user) {
                    window.netlifyIdentity.on("login", () => { document.location.href = "/admin/"; });
                }
            });
        }
    </script>
    <script src="/js/app.js"></script>
</body>
</html>
