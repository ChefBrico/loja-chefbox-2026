<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | ChefBrico Gourmet Prático</title>
    
    <style>
        /* FAIXA COM ALTURA SEGURA (RESOLVE O BOTÃO SUMIDO) */
        .chefbox-sticky-bar {
            position: fixed; 
            bottom: 0; 
            left: 0; 
            width: 100%;
            background: #ffffff; 
            border-top: 5px solid #F2811D;
            padding: 20px 0; /* Altura aumentada para o botão aparecer */
            z-index: 9999;
            box-shadow: 0 -10px 40px rgba(0,0,0,0.2);
            display: flex; 
            flex-direction: row; 
            align-items: center; 
            justify-content: center; 
            gap: 40px;
            backdrop-filter: blur(15px);
        }

        .bar-info-text { text-align: left; line-height: 1.4; min-width: 180px; }
        .bar-info-text b { color: #333; font-size: 1rem; display: block; text-transform: uppercase; }
        .bar-info-text span { color: #F2811D; font-size: 1.1rem; font-weight: 900; }

        .slots-wrapper { display: flex; gap: 12px; align-items: center; }

        /* CÍRCULOS COM IMAGENS (CORREÇÃO DO FUNDO PRETO) */
        .slot-circle {
            width: 55px; height: 55px; 
            border: 2px dashed #ddd; 
            border-radius: 50%;
            display: flex; 
            align-items: center; 
            justify-content: center;
            font-size: 0.9rem; 
            color: #999; 
            background-color: #f9f9f9;
            background-size: cover !important; 
            background-position: center !important;
            background-repeat: no-repeat !important;
            position: relative;
        }

        .slot-circle.filled { border: 3px solid #F2811D; color: transparent !important; }
        .slot-circle.gift { border: 3px solid #25D366; color: #25D366; font-size: 1.5rem; }

        /* BOTÃO DE FINALIZAÇÃO - ONDE RECUPERAMOS A SETA */
        .btn-finish-float {
            background: #25D366 !important; 
            color: white !important; 
            border: none;
            padding: 16px 40px; 
            border-radius: 50px; 
            font-weight: 900;
            cursor: pointer; 
            animation: pulse-war 2s infinite;
            
            /* FORÇAMOS A EXIBIÇÃO EM LINHA PARA A SETA NÃO CAIR */
            display: none; /* Ativado pelo app.js */
            align-items: center;
            justify-content: center;
            gap: 12px;
            
            font-size: 1.1rem;
            white-space: nowrap; 
            box-shadow: 0 10px 25px rgba(37,211,102,0.4);
            text-transform: uppercase;
        }

        @keyframes pulse-war { 
            0% { transform: scale(1); } 
            50% { transform: scale(1.03); } 
            100% { transform: scale(1); } 
        }

        @media (max-width: 992px) {
            .chefbox-sticky-bar { flex-direction: column; padding: 25px 0; gap: 15px; }
            .btn-finish-float { width: 90%; }
        }
    </style>
</head>
<body>
    {% include "header.njk" %}
    <main role="main">{{ content | safe }}</main>
    {% include "footer.njk" %}
    
    {% if '/receitas/' in page.url or '/chefbox/' in page.url %}
    <div id="chefbox-bar" class="chefbox-sticky-bar">
        <div class="bar-info-text">
            <b>🎁 COMBO VIP ATIVADO!</b>
            <span>Total: R$ 132,00</span>
        </div>
        <div class="slots-wrapper">
            <div id="slot-1" class="slot-circle">1</div>
            <div id="slot-2" class="slot-circle">2</div>
            <div id="slot-3" class="slot-circle">3</div>
            <div id="slot-4" class="slot-circle">4</div>
            <div id="slot-5" class="slot-circle gift">🎁</div>
        </div>
        
        <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()">
            Finalizar Pedido R$ 132 ➜
        </button>
    </div>
    {% endif %}

    <div id="checkout-modal" class="modal-overlay" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.9); z-index: 10001; align-items: center; justify-content: center;">
        <div class="modal-box" style="border-top: 8px solid #F2811D; border-radius: 24px; padding: 40px; background: white; max-width: 480px; width: 95%;">
            <h3 style="color: #014039; margin: 0 0 10px;">Finalizar sua ChefBox 🛵</h3>
            <form id="form-checkout-vip" onsubmit="event.preventDefault(); sendOrderToWhatsApp();">
                <input type="text" id="customer-name" placeholder="Nome Completo" required style="width: 100%; padding: 15px; margin-bottom: 12px; border: 1px solid #eee; border-radius: 12px;">
                <input type="tel" id="customer-phone" placeholder="WhatsApp (DDD + Número)" required style="width: 100%; padding: 15px; margin-bottom: 12px; border: 1px solid #eee; border-radius: 12px;">
                <textarea id="customer-address" rows="3" placeholder="Endereço de Entrega Completo" required style="width: 100%; padding: 15px; margin-bottom: 25px; border: 1px solid #eee; border-radius: 12px; resize: none;"></textarea>
                <button type="submit" style="width: 100%; padding: 22px; background: #25D366; color: white; border: none; border-radius: 50px; font-weight: 900; font-size: 1.2rem;">ENVIAR PEDIDO AGORA ➜</button>
                <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 25px; color: #999; cursor: pointer; text-decoration: underline;">Voltar</p>
            </form>
        </div>
    </div>
    
    <script src="/js/app.js"></script>
    <script>
      function openCheckoutModal() { document.getElementById('checkout-modal').style.display = 'flex'; }
      function closeCheckoutModal() { document.getElementById('checkout-modal').style.display = 'none'; }
    </script>
</body>
</html>
