<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{{ title }} | ChefBrico Gourmet Prático</title>
    <meta name="description" content="{{ description }}">
    <meta name="author" content="Maria Conceição">
    <meta name="theme-color" content="#014039">
    
    <link rel="llms-txt" href="/llms.txt">
    <link rel="full-llms-txt" href="/full-llms.txt">
    <link rel="author" href="https://gourmetpratico.com.br/comida-de-verdade/">
    
    <link rel="canonical" href="https://gourmetpratico.com.br{{ page.url }}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://gourmetpratico.com.br{{ page.url }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ description }}">
    
    {% if image %}
    <meta property="og:image" content="https://gourmetpratico.com.br{{ image }}">
    {% else %}
    <meta property="og:image" content="https://gourmetpratico.com.br/imagens/share-padrao-chefbrico.png">
    {% endif %}

    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        .chefbox-sticky-bar {
            position: fixed; bottom: 0; left: 0; width: 100%;
            background: rgba(255, 255, 255, 0.98); border-top: 3px solid #F2811D;
            padding: 10px 0; z-index: 999;
            box-shadow: 0 -5px 20px rgba(0,0,0,0.1);
            display: flex; flex-direction: row; 
            align-items: center; justify-content: center; 
            gap: 30px;
            backdrop-filter: blur(10px);
        }

        .bar-info-text {
            text-align: left;
            line-height: 1.2;
        }

        .bar-info-text b { color: #333; font-size: 0.85rem; display: block; text-transform: uppercase; }
        .bar-info-text span { color: #666; font-size: 0.8rem; font-weight: bold; }

        .slots-wrapper { display: flex; gap: 10px; align-items: center; }

        /* AJUSTE PARA A IMAGEM APARECER: background-size cover é essencial */
        .slot-circle {
            width: 45px; height: 45px; 
            border: 2px dashed #ddd; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.7rem; color: #bbb; background: #fff;
            background-size: cover; /* Faz a imagem ocupar todo o círculo */
            background-position: center;
            background-repeat: no-repeat;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .slot-circle.filled { 
            border: 2px solid #F2811D; 
            color: transparent; /* Esconde o número quando tem imagem */
        }

        .slot-circle.gift { border: 2px solid #25D366; color: #25D366; font-size: 1.1rem; }

        .btn-finish-float {
            background: #25D366; color: white; border: none;
            padding: 12px 30px; border-radius: 50px; font-weight: 800;
            cursor: pointer; animation: pulse 2s infinite;
            display: none; font-size: 0.95rem;
            box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3);
        }

        @keyframes pulse { 0% { transform: scale(1); } 50% { transform: scale(1.05); } 100% { transform: scale(1); } }

        @media (max-width: 850px) {
            .chefbox-sticky-bar { flex-direction: column; gap: 10px; padding: 15px 0; }
            .bar-info-text { text-align: center; }
            .btn-finish-float { width: 90%; }
        }
    </style>

    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    {% if schema %}
    <script type="application/ld+json">
        {{ schema | dump | safe }}
    </script>
    {% endif %}
</head>
<body>
    
    {% include "header.njk" %}
    
    <main role="main">
        {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
    
    {% if '/receitas/' in page.url or '/chefbox/' in page.url %}
    <div id="chefbox-bar" class="chefbox-sticky-bar">
        
        <div class="bar-info-text">
            <b>🎁 Combo VIP Ativado!</b>
            <span>Total Fixo: R$ 132,00</span>
        </div>
        
        <div class="slots-wrapper">
            <div id="slot-1" class="slot-circle">1</div>
            <div id="slot-2" class="slot-circle">2</div>
            <div id="slot-3" class="slot-circle">3</div>
            <div id="slot-4" class="slot-circle">4</div>
            <div id="slot-5" class="slot-circle gift">🎁</div>
        </div>

        <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()">
            Finalizar Pedido R$ 132 ➜
        </button>
    </div>
    {% endif %}

    <div id="checkout-modal" class="modal-overlay" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); z-index: 10000; align-items: center; justify-content: center;">
        <div class="modal-box" style="border-top: 6px solid #F2811D; border-radius: 20px; padding: 35px; background: white; max-width: 450px; width: 90%;">
            <h3 style="color: #014039; margin: 0 0 10px;">Finalizar sua ChefBox 🛵</h3>
            <p style="font-size: 0.9rem; color: #666; margin-bottom: 25px;">Garanta seu <b>Mimo VIP</b> e agende sua entrega no DF.</p>
            
            <form id="form-checkout-vip" onsubmit="event.preventDefault(); sendOrderToWhatsApp();">
                <input type="text" id="customer-name" placeholder="Nome Completo" required style="width: 100%; padding: 14px; margin-bottom: 12px; border: 1px solid #ddd; border-radius: 10px; font-size: 1rem;">
                <input type="tel" id="customer-phone" placeholder="WhatsApp (DDD + Número)" required style="width: 100%; padding: 14px; margin-bottom: 12px; border: 1px solid #ddd; border-radius: 10px; font-size: 1rem;">
                <input type="email" id="customer-email" placeholder="Seu melhor e-mail" required style="width: 100%; padding: 14px; margin-bottom: 12px; border: 1px solid #ddd; border-radius: 10px; font-size: 1rem;">
                
                <div style="display:flex; gap:10px; margin-bottom: 12px;">
                    <input type="text" id="customer-cep" placeholder="CEP (DF)" required style="width: 40%; padding: 14px; border: 1px solid #ddd; border-radius: 10px;">
                    <div style="width: 60%; background:#e8f5e9; color:#2e7d32; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 0.75rem; text-align:center;">
                        🚚 FRETE GRÁTIS DF
                    </div>
                </div>

                <textarea id="customer-address" rows="3" placeholder="Endereço Completo" required style="width: 100%; padding: 14px; margin-bottom: 20px; border: 1px solid #ddd; border-radius: 10px; resize: none; font-size: 1rem;"></textarea>
                
                <button type="submit" style="width: 100%; padding: 20px; background: #25D366; color: white; border: none; border-radius: 50px; font-weight: 900; cursor: pointer; font-size: 1.1rem; box-shadow: 0 5px 15px rgba(37,211,102,0.4);">
                    GARANTIR MEU PRESENTE ➜
                </button>
                
                <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 20px; color: #999; cursor: pointer; text-decoration: underline; font-size: 0.85rem;">Voltar e Alterar Itens</p>
            </form>
        </div>
    </div>
    
    <script>
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on("init", user => {
          if (!user) {
            window.netlifyIdentity.on("login", () => { document.location.href = "/admin/"; });
          }
        });
      }
      function openCheckoutModal() { document.getElementById('checkout-modal').style.display = 'flex'; }
      function closeCheckoutModal() { document.getElementById('checkout-modal').style.display = 'none'; }
    </script>
    <script src="/js/app.js"></script>

</body>
</html>
