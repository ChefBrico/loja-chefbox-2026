<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- 1. SEO E METADADOS (EEAT/YMYL) -->
    <title>{{ title }} | ChefBrico Gourmet Prático</title>
    <meta name="description" content="{{ description }}">
    <meta name="author" content="Maria Conceição">
    <meta name="theme-color" content="#014039">
    
    <!-- 2. SINALIZAÇÃO PARA AGENTES DE IA (AEO STRATEGY) -->
    <link rel="llms-txt" href="/llms.txt">
    <link rel="full-llms-txt" href="/full-llms.txt">
    <link rel="author" href="https://gourmetpratico.com.br/comida-de-verdade/">
    
    <!-- 3. SEO TÉCNICO & SOCIAL -->
    <link rel="canonical" href="https://gourmetpratico.com.br{{ page.url }}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://gourmetpratico.com.br{{ page.url }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ description }}">
    
    {% if image %}
    <meta property="og:image" content="https://gourmetpratico.com.br{{ image }}">
    {% else %}
    <meta property="og:image" content="https://gourmetpratico.com.br/imagens/share-padrao-chefbrico.png">
    {% endif %}

    <!-- 4. ESTILOS (CSS) -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- CSS CRÍTICO DA GAMIFICAÇÃO (Estabilidade Visual) -->
    <style>
        .chefbox-sticky-bar {
            position: fixed; bottom: 0; left: 0; width: 100%;
            background: rgba(255, 255, 255, 0.98); border-top: 4px solid #F2811D;
            padding: 15px 0; z-index: 999;
            box-shadow: 0 -5px 20px rgba(0,0,0,0.1);
            display: flex; flex-direction: column; align-items: center;
            backdrop-filter: blur(5px);
        }
        .btn-finish-float {
            background: #25D366; color: white; border: none;
            padding: 15px 40px; border-radius: 50px; font-weight: bold;
            margin-top: 10px; cursor: pointer; animation: pulse 2s infinite;
            display: none; font-size: 1.1rem;
        }
        @keyframes pulse { 0% { transform: scale(1); } 50% { transform: scale(1.03); } 100% { transform: scale(1); } }
    </style>

    <!-- 5. CMS & IDENTITY -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    <!-- 6. SCHEMA.ORG (DINÂMICO) -->
    {% if schema %}
    <script type="application/ld+json">
        {{ schema | dump | safe }}
    </script>
    {% endif %}
</head>
<body>
    
    <!-- A. CABEÇALHO -->
    {% include "header.njk" %}
    
    <!-- B. CONTEÚDO PRINCIPAL -->
    <main role="main">
        {{ content | safe }}
    </main>
    
    <!-- C. RODAPÉ -->
    {% include "footer.njk" %}
    
    <!-- ======================================================= -->
    <!-- D. RÉGUA DE GAMIFICAÇÃO (SINCRO V7.0)                   -->
    <!-- ======================================================= -->
    {% if '/receitas/' in page.url or '/chefbox/' in page.url %}
    <div id="chefbox-bar" class="chefbox-sticky-bar" data-agent-rule="buy_4_get_1_free">
        <div class="bar-info" style="margin-bottom: 8px; font-size: 0.9rem; color: #333; font-weight: bold;">
            <span id="game-status-text">Monte sua ChefBox VIP:</span>
        </div>
        
        <div class="slots-wrapper" style="display: flex; gap: 10px;">
            <div id="slot-1" class="slot-circle">1</div>
            <div id="slot-2" class="slot-circle">2</div>
            <div id="slot-3" class="slot-circle">3</div>
            <div id="slot-4" class="slot-circle">4</div>
            <div id="slot-5" class="slot-circle gift" title="Seu Presente!">🎁</div>
        </div>

        <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()">
            Finalizar Pedido R$ 132 ➜
        </button>
    </div>
    {% endif %}

    <!-- ======================================================= -->
    <!-- E. MODAL DE CHECKOUT VIP (CRM & n8n READY)              -->
    <!-- ======================================================= -->
    <div id="checkout-modal" class="modal-overlay" style="display: none;">
        <div class="modal-box" style="border-top: 6px solid #F2811D; border-radius: 16px; padding: 30px; background: white; max-width: 450px; width: 90%;">
            <h3 style="color: #014039; margin-top: 0;">Finalizar sua ChefBox 🛵</h3>
            <p style="font-size: 0.85rem; color: #666; margin-bottom: 20px;">
                Complete para garantir seu <strong>Mimo VIP</strong> e agendar sua entrega no DF.
            </p>
            
            <form id="form-checkout-vip" onsubmit="event.preventDefault(); sendOrderToWhatsApp();">
                <input type="text" id="customer-name" placeholder="Nome Completo" required class="input-field" style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
                
                <input type="tel" id="customer-phone" placeholder="WhatsApp (DDD + Número)" required class="input-field" style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">

                <input type="email" id="customer-email" placeholder="Seu melhor e-mail (Obrigatório para o Clube)" required class="input-field" style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
                
                <div style="display:flex; gap:10px; margin-bottom: 10px;">
                    <input type="text" id="customer-cep" placeholder="CEP (DF)" required class="input-field" style="width: 40%; padding: 12px; border: 1px solid #ddd; border-radius: 8px;">
                    <div style="width: 60%; background:#e8f5e9; color:#2e7d32; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 0.7rem; text-align:center;">
                        🚚 FRETE GRÁTIS DISTRITO FEDERAL
                    </div>
                </div>

                <textarea id="customer-address" rows="3" placeholder="Endereço Completo (Rua, Bloco, Número, Bairro)" required class="input-field" style="width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid #ddd; border-radius: 8px; resize: none;"></textarea>
                
                <label style="display: flex; align-items: flex-start; gap: 10px; margin-bottom: 20px; cursor: pointer; font-size: 0.8rem; color: #555;">
                    <input type="checkbox" id="customer-optin" checked style="margin-top: 3px;">
                    <span>Aceito participar do Clube Gourmet Prático e receber mimos da Maria no WhatsApp.</span>
                </label>

                <button type="submit" class="btn-finalizar-zap" style="width: 100%; padding: 18px; background: #25D366; color: white; border: none; border-radius: 50px; font-weight: bold; cursor: pointer; font-size: 1.1rem; box-shadow: 0 4px 15px rgba(37, 211, 102, 0.3);">
                    GARANTIR MEU PRESENTE ➜
                </button>
                
                <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 15px; color: #999; cursor: pointer; text-decoration: underline; font-size: 0.8rem;">Voltar e Alterar Itens</p>
            </form>
        </div>
    </div>
    
    <!-- F. SCRIPTS -->
    <script>
      // Netlify Identity
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on("init", user => {
          if (!user) {
            window.netlifyIdentity.on("login", () => { document.location.href = "/admin/"; });
          }
        });
      }
    </script>
    <script src="/js/app.js"></script>

</body>
</html>
