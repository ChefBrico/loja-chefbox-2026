<!DOCTYPE html>
<html lang="{{ meta.lang or 'pt-BR' }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{{ title }} | {{ meta.siteName }}</title>
    <meta name="description" content="{{ description or meta.description }}">
    <meta name="theme-color" content="#FFFFFF">
    <link rel="canonical" href="{{ meta.url }}{{ page.url }}">

    <meta property="og:title" content="{{ title }} | ChefBrico">
    <meta property="og:description" content="{{ description or meta.description }}">
    <meta property="og:image" content="{{ meta.url }}{{ image or meta.image }}">
    <meta property="og:url" content="{{ meta.url }}{{ page.url }}">
    <meta property="og:type" content="website">

    <link rel="alternate" type="text/plain" title="LLM Instructions" href="{{ meta.agent.llms_txt }}">
    <link rel="author" href="https://gourmetpratico.com.br/comida-de-verdade/">

    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        /* --- ESTILOS DE INTERFACE PREMIUM (APPLE STYLE) --- */
        :root {
            --chef-orange: #F2811D;
            --chef-green: #014039;
            --whatsapp-green: #25D366;
            --apple-cream: #FDFBF7;
            --glass-bg: rgba(255, 255, 255, 0.85);
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            -webkit-font-smoothing: antialiased;
            background-color: #FFFFFF;
        }

        /* 1. RÉGUA CHEFBOX (FLOATING SYSTEM) */
        .chefbox-sticky-bar {
            position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%);
            width: 92%; max-width: 600px;
            background: var(--glass-bg);
            backdrop-filter: blur(15px) saturate(180%);
            -webkit-backdrop-filter: blur(15px) saturate(180%);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-top: 4px solid var(--chef-orange);
            border-radius: 30px;
            padding: 15px 25px; z-index: 1000;
            box-shadow: 0 20px 50px rgba(0,0,0,0.15);
            display: flex; align-items: center; justify-content: space-between;
            gap: 15px;
        }

        .bar-info b { color: var(--chef-green); font-size: 0.75rem; letter-spacing: 1px; }
        #visor-dinamico-lucro { font-size: 0.85rem; font-weight: 900; color: var(--chef-orange); display: block; }

        .slots-wrapper { display: flex; gap: 8px; align-items: center; }

        .slot-circle {
            width: 42px; height: 42px; border: 2px dashed #ddd; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.7rem; color: #bbb; background: #fff;
            background-size: cover !important; background-position: center !important;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        .slot-circle.filled { border: 2.5px solid var(--chef-orange); box-shadow: 0 5px 15px rgba(242, 129, 29, 0.2); }
        .slot-circle.gift { border: 2.5px solid var(--whatsapp-green); color: var(--whatsapp-green); font-size: 1.1rem; }

        .btn-finish-float {
            background: var(--whatsapp-green); color: white; border: none;
            padding: 12px 22px; border-radius: 50px; font-weight: 900;
            cursor: pointer; font-size: 0.85rem; text-transform: uppercase;
            box-shadow: 0 8px 20px rgba(37, 211, 102, 0.3);
            white-space: nowrap; animation: pulse-apple 2s infinite;
        }

        @keyframes pulse-apple { 
            0% { transform: scale(1); box-shadow: 0 8px 20px rgba(37, 211, 102, 0.3); } 
            50% { transform: scale(1.03); box-shadow: 0 12px 30px rgba(37, 211, 102, 0.4); } 
            100% { transform: scale(1); box-shadow: 0 8px 20px rgba(37, 211, 102, 0.3); } 
        }

        /* 2. MODAL DE CHECKOUT (PREMIUM LOOK) */
        .modal-overlay {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(1, 64, 57, 0.6); backdrop-filter: blur(8px); z-index: 2000;
            align-items: center; justify-content: center;
        }
        .modal-box {
            background: white; border-radius: 32px; padding: 40px;
            max-width: 460px; width: 90%; 
            box-shadow: 0 30px 60px rgba(0,0,0,0.2);
            border: 1px solid rgba(255,255,255,0.2);
            text-align: center;
        }
        .input-field { 
            width: 100%; padding: 14px; margin-bottom: 12px; 
            border: 1px solid #eee; border-radius: 14px; 
            font-size: 1rem; background: #F9F9F9;
            box-sizing: border-box; transition: border 0.3s;
        }
        .input-field:focus { border-color: var(--chef-orange); outline: none; background: #fff; }

        @media (max-width: 768px) {
            .chefbox-sticky-bar { width: 95%; padding: 10px 15px; bottom: 10px; }
            .bar-info { display: none; }
            .slot-circle { width: 38px; height: 38px; }
            .btn-finish-float { padding: 10px 16px; font-size: 0.75rem; }
        }
    </style>

    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body>
    
    {% include "header.njk" %}
    
    <main role="main">
        {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
    
    {% if '/receitas/' in page.url or '/chefbox/' in page.url or '/onde-encontrar/' in page.url %}
    <div id="chefbox-bar" class="chefbox-sticky-bar">
        <div class="bar-info">
            <div id="game-status-text">
                <b>🎁 CHEFBOX VIP</b>
                <span id="visor-dinamico-lucro">ECONOMIA R$ 0,00</span>
            </div>
        </div>
        
        <div class="slots-wrapper">
            <div id="slot-1" class="slot-circle">1</div>
            <div id="slot-2" class="slot-circle">2</div>
            <div id="slot-3" class="slot-circle">3</div>
            <div id="slot-4" class="slot-circle">4</div>
            <div id="slot-5" class="slot-circle gift">🎁</div>
        </div>

        <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()">
            Finalizar R$ 132 ➜
        </button>
    </div>
    {% endif %}

    <div id="checkout-modal" class="modal-overlay">
        <div class="modal-box">
            <h3 style="color: var(--chef-green); margin-bottom: 10px; font-family: 'Merriweather', serif; font-size: 1.8rem;">Quase lá! 🛵</h3>
            <p style="font-size: 0.95rem; color: #666; margin-bottom: 25px;">
                Preencha os dados para a Maria preparar seu <b>Mimo VIP</b> e agendar sua entrega (D+1).
            </p>
            
            <form id="form-checkout-vip" onsubmit="event.preventDefault(); sendOrderToWhatsApp();">
                <input type="text" id="customer-name" placeholder="Nome Completo" required class="input-field">
                <input type="tel" id="customer-phone" placeholder="WhatsApp (DDD + Número)" required class="input-field">
                <input type="email" id="customer-email" placeholder="E-mail para confirmação" required class="input-field">
                
                <div style="display:flex; gap:10px; margin-bottom: 12px;">
                    <input type="text" id="customer-cep" placeholder="CEP (DF)" required style="width: 45%; padding: 14px; border: 1px solid #eee; border-radius: 14px; background: #F9F9F9;">
                    <div style="width: 55%; background:#e8f5e9; color:#2e7d32; border-radius: 14px; display: flex; align-items: center; justify-content: center; font-weight: 800; font-size: 0.65rem; text-align:center; text-transform: uppercase;">
                        ✅ FRETE GRÁTIS NO DF
                    </div>
                </div>

                <textarea id="customer-address" rows="3" placeholder="Endereço Completo (Rua, Bloco, Número, Bairro)" required class="input-field" style="resize: none;"></textarea>
                
                <button type="submit" style="width: 100%; padding: 20px; background: var(--whatsapp-green); color: white; border: none; border-radius: 50px; font-weight: 900; cursor: pointer; font-size: 1.1rem; box-shadow: 0 10px 25px rgba(37, 211, 102, 0.4); margin-top: 10px;">
                    GARANTIR MEU PRESENTE ➜
                </button>
                
                <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 20px; color: #999; cursor: pointer; text-decoration: underline; font-size: 0.8rem;">Revisar Sabores</p>
            </form>
        </div>
    </div>

    <script>
        function openCheckoutModal() { document.getElementById('checkout-modal').style.display = 'flex'; }
        function closeCheckoutModal() { document.getElementById('checkout-modal').style.display = 'none'; }

        if (window.netlifyIdentity) {
            window.netlifyIdentity.on("init", user => {
                if (!user) {
                    window.netlifyIdentity.on("login", () => { document.location.href = "/admin/"; });
                }
            });
        }
    </script>
    <script src="/js/app.js"></script>
</body>
</html>
