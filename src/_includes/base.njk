<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- 1. SEO E METADADOS -->
    <title>{{ title }} | ChefBrico</title>
    <meta name="description" content="{{ description }}">
    <meta name="author" content="Maria Conceição">
    
    <!-- 2. SEO TÉCNICO -->
    <link rel="canonical" href="https://gourmetpratico.com.br{{ page.url }}">

    <!-- 3. REDES SOCIAIS (Open Graph) -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://gourmetpratico.com.br{{ page.url }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ description }}">
    
    {% if image %}
    <meta property="og:image" content="https://gourmetpratico.com.br{{ image }}">
    {% else %}
    <meta property="og:image" content="https://gourmetpratico.com.br/imagens/logo.png">
    {% endif %}

    <!-- 4. A ROUPA DO SITE (CSS) -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- 5. O PORTEIRO DO CMS -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    <!-- 6. SCHEMA.ORG -->
    {% if schema %}
    <script type="application/ld+json">
        {{ schema | safe }}
    </script>
    {% endif %}
</head>
<body>
    
    <!-- A. CABEÇALHO -->
    {% include "header.njk" %}
    
    <!-- B. CONTEÚDO PRINCIPAL -->
    <main>
        {{ content | safe }}
    </main>
    
    <!-- C. RODAPÉ -->
    {% include "footer.njk" %}
    
    <!-- ======================================================= -->
    <!-- D. A-COMMERCE: RÉGUA DE GAMIFICAÇÃO (MECANISMO 4+1)     -->
    <!-- BLINDADA: Estilos inline para forçar horizontalidade    -->
    <!-- ======================================================= -->
    
    {% if '/receitas/' in page.url or '/chefbox/' in page.url %}
    <div id="chefbox-bar" class="chefbox-sticky-bar" style="display: flex !important; flex-direction: column !important; align-items: center !important; padding: 10px 0 !important;">
        
        <!-- Texto (Fica em cima) -->
        <div class="bar-info" style="margin-bottom: 5px !important; font-size: 0.8rem !important;">
            <span id="game-status-text">Monte sua ChefBox:</span>
        </div>
        
        <!-- Bolinhas (FORÇADO A FICAR EM LINHA / HORIZONTAL) -->
        <div class="slots-wrapper" style="display: flex !important; flex-direction: row !important; flex-wrap: nowrap !important; gap: 10px !important; justify-content: center !important; width: 100% !important;">
            <div id="slot-1" class="slot-circle">1</div>
            <div id="slot-2" class="slot-circle">2</div>
            <div id="slot-3" class="slot-circle">3</div>
            <div id="slot-4" class="slot-circle">4</div>
            <div id="slot-5" class="slot-circle gift">🎁</div>
        </div>

        <!-- Botão Finalizar -->
        <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()" style="display:none; margin-top: 5px;">
            Finalizar Pedido ➜
        </button>
    </div>
    {% endif %}

   <!-- ======================================================= -->
    <!-- E. A-COMMERCE: MODAL DE CHECKOUT (SACOLINHA & DADOS)    -->
    <!-- ======================================================= -->
    <div id="checkout-modal" class="modal-overlay" style="display: none;">
        <div class="modal-box">
            <h3 style="color: #014039; text-align: center; margin-bottom: 15px;">Seu Pedido ChefBox 👩‍🍳</h3>
            
            <!-- 1. A SACOLINHA (RESUMO VISUAL) -->
            <div id="cart-summary" style="background: #f9f9f9; padding: 15px; border-radius: 8px; margin-bottom: 20px; max-height: 200px; overflow-y: auto; border: 1px solid #eee;">
                <!-- O Javascript vai preencher isso aqui -->
                <p style="text-align:center; color:#999;">Carregando itens...</p>
            </div>

            <!-- 2. DADOS DO CLIENTE -->
            <p style="font-size: 0.9rem; color: #666; margin-bottom: 10px;">Seus dados para entrega e contato:</p>
            
            <input type="text" id="customer-name" placeholder="Nome Completo" style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
            
            <!-- NOVO CAMPO: WHATSAPP -->
            <input type="tel" id="customer-phone" placeholder="Seu WhatsApp (com DDD)" style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
            
            <input type="text" id="customer-cep" placeholder="CEP" style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
            
            <textarea id="customer-address" placeholder="Endereço Completo (Rua, Número, Complemento)" rows="3" style="width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid #ddd; border-radius: 8px;"></textarea>
            
            <!-- 3. BOTÃO DE ENVIO -->
            <button onclick="sendOrderToWhatsApp()" style="width: 100%; padding: 15px; background: #25D366; color: white; border: none; border-radius: 50px; font-weight: bold; font-size: 1.1rem; cursor: pointer; box-shadow: 0 4px 10px rgba(37, 211, 102, 0.3);">
                FINALIZAR PEDIDO NO WHATSAPP 💬
            </button>
            
            <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 15px; color: #999; cursor: pointer; text-decoration: underline;">Voltar / Alterar Itens</p>
        </div>
    </div>
    
    <!-- F. SCRIPTS DO SISTEMA -->
    
    <!-- Script de Login do Admin (Netlify) - CORRIGIDO -->
    <script>
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on("init", user => {
          if (!user) {
            window.netlifyIdentity.on("login", () => {
              document.location.href = "/admin/";
            });
          }
        });
      }
    </script>

    <!-- Script de Segurança para exibir a régua -->
    <script>
        window.onload = function() {
            const bar = document.querySelector('.chefbox-sticky-bar');
            if(bar) bar.style.display = 'flex';
        }
    </script>

    <!-- O CÉREBRO DO JOGO (Carrega por último) -->
    <script src="/js/app.js"></script>

</body>
</html>
