<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{{ title }} | ChefBrico Gourmet Prático</title>
    <meta name="description" content="{{ description }}">
    <meta name="author" content="Maria Conceição">
    <meta name="theme-color" content="#014039">
    
    <link rel="llms-txt" href="/llms.txt">
    <link rel="full-llms-txt" href="/full-llms.txt">
    <link rel="author" href="https://gourmetpratico.com.br/comida-de-verdade/">
    
    <link rel="canonical" href="https://gourmetpratico.com.br{{ page.url }}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://gourmetpratico.com.br{{ page.url }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ description }}">
    
    {% if image %}
    <meta property="og:image" content="https://gourmetpratico.com.br{{ image }}">
    {% else %}
    <meta property="og:image" content="https://gourmetpratico.com.br/imagens/share-padrao-chefbrico.png">
    {% endif %}

    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        .chefbox-sticky-bar {
            position: fixed; 
            bottom: 0; 
            left: 0; 
            width: 100%;
            background: rgba(255, 255, 255, 0.96); 
            border-top: 3px solid #F2811D;
            padding: 8px 0; /* Altura reduzida conforme solicitado */
            z-index: 999;
            box-shadow: 0 -5px 20px rgba(0,0,0,0.1);
            display: flex; 
            flex-direction: row; /* Horizontal para Desktop */
            align-items: center; 
            justify-content: center; /* Centralização Absoluta */
            gap: 25px;
            backdrop-filter: blur(10px);
        }

        .bar-info-compact {
            font-size: 0.8rem; 
            color: #333; 
            font-weight: 800; 
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .slots-wrapper { 
            display: flex; 
            gap: 12px; /* Espaçamento harmônico entre slots */
            align-items: center;
        }

        .slot-circle {
            width: 38px; height: 38px; /* Tamanho otimizado */
            border: 2px dashed #ccc; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.8rem; color: #999; background: #fff;
            transition: all 0.3s ease;
        }

        .slot-circle.filled { border: 2px solid #F2811D; background: #fffcf5; }
        .slot-circle.gift { border: 2px solid #F2811D; color: #F2811D; font-size: 1.2rem; }

        .btn-finish-float {
            background: #25D366; 
            color: white; 
            border: none;
            padding: 10px 25px; 
            border-radius: 50px; 
            font-weight: bold;
            cursor: pointer; 
            animation: pulse 2s infinite;
            display: none; 
            font-size: 0.9rem;
            box-shadow: 0 4px 10px rgba(37, 211, 102, 0.3);
        }

        @keyframes pulse { 0% { transform: scale(1); } 50% { transform: scale(1.05); } 100% { transform: scale(1); } }

        /* Ajuste Mobile: Garante que não quebre em telas pequenas */
        @media (max-width: 768px) {
            .chefbox-sticky-bar { 
                flex-direction: column; 
                padding: 12px 0; 
                gap: 8px; 
            }
            .btn-finish-float { margin-top: 5px; width: 90%; }
        }
    </style>

    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    {% if schema %}
    <script type="application/ld+json">
        {{ schema | dump | safe }}
    </script>
    {% endif %}
</head>
<body>
    
    {% include "header.njk" %}
    
    <main role="main">
        {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
    
    {% if '/receitas/' in page.url or '/chefbox/' in page.url %}
    <div id="chefbox-bar" class="chefbox-sticky-bar" data-agent-rule="buy_4_get_1_free">
        
        <div class="bar-info-compact">
            <span id="game-status-text">Combo VIP Ativado:</span>
        </div>
        
        <div class="slots-wrapper">
            <div id="slot-1" class="slot-circle">1</div>
            <div id="slot-2" class="slot-circle">2</div>
            <div id="slot-3" class="slot-circle">3</div>
            <div id="slot-4" class="slot-circle">4</div>
            <div id="slot-5" class="slot-circle gift" title="Seu Presente!">🎁</div>
        </div>

        <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()">
            Finalizar Pedido R$ 132 ➜
        </button>
    </div>
    {% endif %}

    <div id="checkout-modal" class="modal-overlay" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.7); z-index: 10000; align-items: center; justify-content: center;">
        <div class="modal-box" style="border-top: 6px solid #F2811D; border-radius: 16px; padding: 30px; background: white; max-width: 450px; width: 90%; position: relative;">
            <h3 style="color: #014039; margin-top: 0;">Finalizar sua ChefBox 🛵</h3>
            <p style="font-size: 0.85rem; color: #666; margin-bottom: 20px;">
                Complete para garantir seu <strong>Mimo VIP</strong> e agendar sua entrega no DF.
            </p>
            
            <form id="form-checkout-vip" onsubmit="event.preventDefault(); sendOrderToWhatsApp();">
                <input type="text" id="customer-name" placeholder="Nome Completo" required style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
                
                <input type="tel" id="customer-phone" placeholder="WhatsApp (DDD + Número)" required style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">

                <input type="email" id="customer-email" placeholder="Seu melhor e-mail" required style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
                
                <div style="display:flex; gap:10px; margin-bottom: 10px;">
                    <input type="text" id="customer-cep" placeholder="CEP (DF)" required style="width: 40%; padding: 12px; border: 1px solid #ddd; border-radius: 8px;">
                    <div style="width: 60%; background:#e8f5e9; color:#2e7d32; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 0.7rem; text-align:center; padding: 5px;">
                        🚚 FRETE GRÁTIS DISTRITO FEDERAL
                    </div>
                </div>

                <textarea id="customer-address" rows="3" placeholder="Endereço Completo" required style="width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid #ddd; border-radius: 8px; resize: none;"></textarea>
                
                <button type="submit" style="width: 100%; padding: 18px; background: #25D366; color: white; border: none; border-radius: 50px; font-weight: bold; cursor: pointer; font-size: 1.1rem;">
                    GARANTIR MEU PRESENTE ➜
                </button>
                
                <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 15px; color: #999; cursor: pointer; text-decoration: underline; font-size: 0.8rem;">Voltar e Alterar Itens</p>
            </form>
        </div>
    </div>
    
    <script>
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on("init", user => {
          if (!user) {
            window.netlifyIdentity.on("login", () => { document.location.href = "/admin/"; });
          }
        });
      }
      function openCheckoutModal() { document.getElementById('checkout-modal').style.display = 'flex'; }
      function closeCheckoutModal() { document.getElementById('checkout-modal').style.display = 'none'; }
    </script>
    <script src="/js/app.js"></script>

</body>
</html>
