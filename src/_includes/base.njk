<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>{{ title }} | ChefBrico Gourmet Prático</title>
    <meta name="description" content="{{ description }}">
    <meta name="theme-color" content="#014039">
    
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        .chefbox-sticky-bar {
            position: fixed; bottom: 0; left: 0; width: 100%;
            background: rgba(255, 255, 255, 0.98); border-top: 4px solid #F2811D;
            /* Altura reduzida com padding vertical mínimo */
            padding: 12px 0; z-index: 999;
            box-shadow: 0 -5px 20px rgba(0,0,0,0.1);
            display: flex; 
            flex-direction: row; /* Força tudo em uma linha horizontal */
            align-items: center; 
            justify-content: center; /* Centraliza o conjunto na página */
            gap: 25px; /* Espaço entre texto, slots e botão */
            backdrop-filter: blur(10px);
        }

        .bar-info { 
            font-size: 0.85rem; 
            color: #333; 
            font-weight: 800; 
            text-transform: uppercase;
            letter-spacing: 0.5px;
            text-align: left;
        }

        .slots-wrapper { 
            display: flex; 
            gap: 12px; 
            align-items: center;
        }

        /* AJUSTE PARA IMAGENS APARECEREM NOS SLOTS */
        .slot-circle {
            width: 45px; height: 45px; /* Tamanho ideal para visibilidade */
            border: 2px dashed #ddd; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.75rem; color: #bbb; background: #fff;
            /* Garante que a imagem preencha o círculo sem fundo preto */
            background-size: cover !important; 
            background-position: center !important;
            background-repeat: no-repeat !important;
            transition: all 0.3s ease;
        }

        .slot-circle.filled { border: 2.5px solid #F2811D; color: transparent; }
        .slot-circle.gift { border: 2.5px solid #25D366; color: #25D366; font-size: 1.2rem; }

        /* BOTÃO INTEGRADO NA LINHA COM SETA PISCANTE */
        .btn-finish-float {
            background: #25D366; color: white; border: none;
            padding: 12px 30px; border-radius: 50px; font-weight: 900;
            cursor: pointer; animation: pulse-war 2s infinite;
            display: none; /* Ativado pelo app.js */
            font-size: 1rem;
            box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3);
            white-space: nowrap;
        }

        @keyframes pulse-war { 
            0% { transform: scale(1); box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3); } 
            50% { transform: scale(1.05); box-shadow: 0 6px 18px rgba(37, 211, 102, 0.5); } 
            100% { transform: scale(1); box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3); } 
        }

        /* Ajuste Mobile para não quebrar a linha horizontal */
        @media (max-width: 768px) {
            .chefbox-sticky-bar { 
                gap: 10px;
                padding: 10px 5px;
            }
            .bar-info { display: none; } /* Esconde o texto no mobile para sobrar espaço */
            .slot-circle { width: 40px; height: 40px; }
            .btn-finish-float { padding: 10px 15px; font-size: 0.85rem; }
        }
    </style>

    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body>
    
    {% include "header.njk" %}
    
    <main role="main">
        {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
    
    {% if '/receitas/' in page.url or '/chefbox/' in page.url %}
<div id="chefbox-bar" class="chefbox-sticky-bar">
    
    <div class="bar-info">
        <div id="game-status-text">
            <b>🎁 COMBO VIP:</b><br>
            <span>Total R$ 132,00</span>
        </div>
    </div>
    
    <div class="slots-wrapper">
        <div id="slot-1" class="slot-circle">1</div>
        <div id="slot-2" class="slot-circle">2</div>
        <div id="slot-3" class="slot-circle">3</div>
        <div id="slot-4" class="slot-circle">4</div>
        <div id="slot-5" class="slot-circle gift">🎁</div>
    </div>

    <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()">
        Finalizar R$ 132 ➜
    </button>
</div>
{% endif %}

    <script src="/js/app.js"></script>
</body>
</html>
