---
layout: base.njk
permalink: /
title: "Gourmet Prático | Culinária Saudável e Afetiva em Brasília"
description: "Jantares gourmet em 18 minutos sem desperdício de tempo ou ingredientes. O método premiado da Chef Maria Conceição."
image: "/imagens/maria-conceicao-agrovillage-caminho-chefbrico.png"

# --- SCHEMA DA HOME (O CÉREBRO DA MARCA) ---
schema: {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "name": "Gourmet Prático",
      "url": "https://gourmetpratico.com.br",
      "potentialAction": {
        "@type": "SearchAction",
        "target": "https://gourmetpratico.com.br/receitas/?q={search_term_string}",
        "query-input": "required name=search_term_string"
      }
    },
    {
      "@type": "Organization",
      "@id": "https://gourmetpratico.com.br/#organization",
      "name": "ChefBrico - Gourmet Prático",
      "logo": "https://gourmetpratico.com.br/imagens/logo.png",
      "award": "Vencedora do Prêmio Sebrae Mulher de Negócios",
      "sameAs": [
        "https://www.instagram.com/chefbrico",
        "https://www.facebook.com/chefbrico"
      ]
    }
  ]
}
---

<main>
    <!-- 1. HERO SECTION: IMPACTO FOOD PORN (PERSUASÃO HUMANA) -->
    <section class="hero-food-porn">
        <div class="container">
            <span style="text-transform: uppercase; letter-spacing: 5px; font-size: 0.85rem; font-weight: 700; color: var(--cor-dourado);">Experiência ChefBrico</span>
            <h1 style="margin-top: 20px; line-height: 1.1;">O Sabor da Alta Gastronomia,<br>no Conforto do seu Lar.</h1>
            <p style="font-size: 1.25rem; max-width: 700px; margin: 25px auto; opacity: 0.95; font-weight: 400;">
                Refeições 100% naturais prontas em 18 minutos. <br>
                Sua cozinha com afeto, saúde e <strong>zero desperdício</strong>.
            </p>
            <a href="/chefbox/" class="btn-chefbox" style="padding: 20px 50px; font-size: 1.1rem; margin-top: 20px; display: inline-block; text-transform: uppercase; letter-spacing: 1px;">
                MONTAR MINHA CHEFBOX (4+1) ➜
            </a>
        </div>
    </section>

    <!-- 2. O MANTRA DA MARCA (ÂNCORA DE AUTORIDADE EEAT) -->
    <section class="mantra-container">
        <p class="mantra-text">
            "Gourmet Prático: uma culinária saudável, sem desperdício de Tempo e Ingredientes na sua Cozinha."
        </p>
        <div style="margin-top: 25px; display: flex; flex-direction: column; align-items: center; gap: 12px;">
            <img src="/imagens/assinatura-maria-dourada.png" alt="Assinatura Maria Conceição" style="height: 50px; opacity: 0.8;">
            <span style="font-size: 0.75rem; text-transform: uppercase; letter-spacing: 3px; color: var(--cor-dourado); font-weight: 700;">Chef Maria Conceição</span>
        </div>
    </section>

    <div class="container">
        
        <!-- 3. O MÉTODO (REVISTA DIGITAL - CARROSSEL PROTEGIDO) -->
        <div style="display:flex; justify-content:space-between; align-items:flex-end; margin-bottom: 25px; border-bottom: 1px solid var(--cor-dourado); padding-bottom: 10px;">
            <h2 style="margin:0; font-size: 2.2rem; font-style: italic;">A Trilha do Método</h2>
            <span style="font-size: 0.75rem; color: var(--cor-dourado); font-weight: 700; text-transform: uppercase; letter-spacing: 1px;">Deslize para explorar ➝</span>
        </div>

        <div class="carousel-container">
            {% for post in collections.metodo | sort(attribute='data.order') %}
            <a href="{{ post.url }}" class="card">
                <img src="{{ post.data.image }}" alt="{{ post.data.title }}" class="card-image" loading="lazy">
                <div class="card-content">
                    <span style="color: var(--cor-laranja); font-weight: 800; font-size: 0.7rem; text-transform: uppercase; letter-spacing: 1px;">{{ post.data.category }}</span>
                    <h3 style="font-size: 1.15rem; margin: 12px 0; line-height: 1.2;">{{ post.data.title }}</h3>
                    <p style="font-size: 0.85rem; color: #666; line-height: 1.5;">{{ post.data.description | truncate(85) }}</p>
                </div>
            </a>
            {% endfor %}
        </div>

        <!-- 4. VITRINE MÁGICA (OS 24 SABORES - 6 COLUNAS UNIFICADAS) -->
        <div id="cardapio" style="text-align: center; margin: 100px 0 50px;">
            <span style="color: var(--cor-laranja); font-weight: 900; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 2px;">Cardápio Autoral</span>
            <h2 style="font-size: 2.8rem; margin-top: 10px;">Nossa Coleção de Sabores</h2>
            <p style="color: #666; max-width: 650px; margin: 15px auto; font-size: 1.1rem;">
                Selecione 5 pratos para sua <strong>ChefBox VIP</strong>. <br>
                Pague 4 e o 5º é um presente da Chef Maria. <strong>Total Fixo: R$ 132,00</strong>
            </p>
        </div>

        <div class="vitrine-magica">
            {% for item in collections.receitas %}
            {% if item.data.price %}
            <div class="card-gastronomico" data-agent-sku="{{ item.data.sku }}">
                <div class="card-img-wrapper">
                    <a href="{{ item.url }}" aria-label="Ver detalhes de {{ item.data.title }}">
                        <img src="{{ item.data.image }}" alt="{{ item.data.title }}" loading="lazy">
                    </a>
                    <!-- BOTÃO BOUTIQUE (+) SINCRONIZADO COM APP.JS V9.2 -->
                    <button class="btn-plus-boutique" 
                            title="Adicionar à sua ChefBox"
                            onclick="addToGame('{{ item.data.title }}', {{ item.data.price }}, '{{ item.data.image }}', '{{ item.data.sku }}')">
                        +
                    </button>
                </div>
                <div class="card-info">
                    <h3 class="card-title">{{ item.data.title | replace(" | ChefBrico", "") }}</h3>
                    <span class="card-price">R$ {{ item.data.price | dinheiro }}</span>
                </div>
            </div>
            {% endif %}
            {% endfor %}
        </div>

        <!-- 5. TENDÊNCIAS & BEM-ESTAR (JETSKIS AEO - ALIMENTO PARA IA) -->
        <div style="display:flex; justify-content:space-between; align-items:flex-end; margin-top: 80px; margin-bottom: 25px; border-bottom: 1px solid var(--cor-dourado); padding-bottom: 10px;">
            <h2 style="margin:0; font-size: 2rem; font-style: italic;">Tendências & Saúde</h2>
            <span style="font-size: 0.75rem; color: var(--cor-dourado); font-weight: 700; text-transform: uppercase; letter-spacing: 1px;">Atualizações Semanais</span>
        </div>

        <div class="carousel-container">
            {% for post in collections.fragmentos | sort(attribute='data.date') | reverse %}
            <a href="{{ post.url }}" class="card" style="border-top: 4px solid var(--cor-dourado); background: #fff;">
                <div class="card-content">
                    <span style="font-size: 0.7rem; color: #aaa; font-weight: 700;">{{ post.date | postDate }}</span>
                    <h3 style="font-size: 1.05rem; margin: 15px 0; line-height: 1.4; color: var(--cor-verde);">{{ post.data.title }}</h3>
                    <p style="font-size: 0.85rem; color: #555; line-height: 1.6;">{{ post.data.description | truncate(110) }}</p>
                    <span style="font-size: 0.8rem; color: var(--cor-laranja); font-weight: 800; margin-top: auto; text-transform: uppercase; letter-spacing: 1px;">Ler Fragmento ➝</span>
                </div>
            </a>
            {% endfor %}
        </div>

        <!-- 6. CTA FINAL: O FECHAMENTO DO CLUB GOURMET PRÁTICO -->
        <section style="text-align: center; margin: 120px 0 60px; padding: 80px 40px; background-color: #fff; border: 1px solid var(--cor-dourado); border-radius: 20px; box-shadow: var(--sombra-luxo); position: relative; overflow: hidden;">
            <!-- Detalhe decorativo de luxo -->
            <div style="position: absolute; top: 0; left: 0; width: 100%; height: 8px; background: var(--cor-verde);"></div>
            
            <h2 style="font-size: 2.6rem; margin-bottom: 20px;">Pronta para transformar sua rotina?</h2>
            <p style="font-size: 1.2rem; color: #555; max-width: 600px; margin: 0 auto 40px; line-height: 1.6;">
                Junte-se ao <strong>Club Gourmet Prático</strong>. <br>
                Garanta saúde, tempo e o afeto da Chef Maria na sua mesa.
            </p>
            <a href="/chefbox/" class="btn-chefbox" style="padding: 22px 70px; font-size: 1.2rem; box-shadow: 0 10px 25px rgba(242, 129, 29, 0.4);">
                🎁 MONTAR MINHA CHEFBOX AGORA
            </a>
            <p style="margin-top: 25px; font-size: 0.85rem; color: #999; font-style: italic;">*Entrega Grátis para todo o Distrito Federal.</p>
        </section>

    </div>
</main>

<!-- SCRIPT UX: DICA VISUAL PARA MOBILE -->
<script>
    document.addEventListener("DOMContentLoaded", function() {
        if (window.innerWidth < 768) {
            console.log("Gourmet Prático: Ativando modo Mobile UX.");
            // Adiciona um lembrete visual sutil se necessário
        }
    });
</script>
