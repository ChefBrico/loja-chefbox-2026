---
layout: base.njk
title: "Monte sua ChefBox | 4 Sabores + 1 Presente"
description: "Escolha seus pratos favoritos. Complete 4 e ganhe o 5º de presente!"
permalink: /chefbox/
---

<style>
/* Estilo Local para a Vitrine ChefBox */
.chefbox-header { text-align: center; padding: 40px 20px; }
.chefbox-header h1 { color: #014039; font-family: 'Merriweather', serif; }
.chefbox-header p { color: #666; font-size: 1.1rem; max-width: 600px; margin: 0 auto; }

/* Grid de Produtos */
.vitrine-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); /* Cards menores e ágeis */
    gap: 15px;
    padding: 0 20px 150px; /* Espaço embaixo para a régua */
    max-width: 1200px;
    margin: 0 auto;
}

.item-card {
    background: white;
    border: 1px solid #eee;
    border-radius: 12px;
    overflow: hidden;
    position: relative;
    box-shadow: 0 4px 10px rgba(0,0,0,0.05);
    transition: transform 0.2s;
}
.item-card:hover { transform: translateY(-3px); border-color: #F2811D; }

.item-img { width: 100%; height: 140px; object-fit: cover; }

.item-info { padding: 10px; }
.item-title { font-size: 0.9rem; font-weight: bold; color: #333; margin: 0 0 5px; line-height: 1.3; height: 2.6em; overflow: hidden; }
.item-price { color: #27ae60; font-weight: 800; font-size: 1rem; }

/* O BOTÃO DE ADICIONAR (+) */
.btn-plus-add {
    position: absolute;
    top: 100px; right: 10px; /* Flutuando sobre a imagem */
    width: 35px; height: 35px;
    background-color: #F2811D;
    color: white;
    border-radius: 50%;
    border: none;
    font-size: 1.5rem;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer;
    box-shadow: 0 3px 6px rgba(0,0,0,0.2);
}
.btn-plus-add:active { transform: scale(0.9); background-color: #d35400; }
</style>

<div class="chefbox-header">
    <h1>Monte sua ChefBox 👩‍🍳</h1>
    <p>Toque no <strong>(+)</strong> para adicionar à caixa. <br>Complete 4 slots e o <strong>5º é Presente!</strong> 🎁</p>
</div>

<!-- A VITRINE DE ESCOLHA (CORRIGIDA) -->
<div class="vitrine-grid">
   {% for item in collections.receitas %}
    
    <div class="item-card">
        <!-- Link Seguro usando item.url -->
        <a href="{{ item.url }}">
            <img src="{{ item.data.image }}" alt="{{ item.data.title }}" class="item-img" loading="lazy">
        </a>
        
        <!-- BOTÃO DO JOGO -->
        <!-- Adicionei o item.url como 5º parâmetro para o botão funcionar no futuro -->
        <button class="btn-plus-add" 
                onclick="addToGame('{{ item.data.title }}', {{ item.data.price }}, '{{ item.data.image }}', '{{ item.data.sku }}', '{{ item.url }}')">
            +
        </button>

        <div class="item-info">
            <a href="{{ item.url }}" style="text-decoration: none; color: inherit;">
                <h3 class="item-title">{{ item.data.title }}</h3>
            </a>
            <div class="item-price">R$ {{ item.data.price | dinheiro }}</div>
        </div>
    </div>
    {% endfor %}
</div>
