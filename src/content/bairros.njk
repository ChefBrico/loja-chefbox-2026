---
pagination:
    data: locations.items
    size: 1
    alias: local
# CORREÇÃO CHAVE: O link agora bate 100% com o seu menu de dominação
permalink: "/onde-encontrar/{{ local.slug }}/"
layout: base.njk
eleventyComputed:
    title: "ChefBrico na {{ local.name }} | Gourmet Prático no DF"
    description: "Atendimento VIP na {{ local.name }}. Peça sua ChefBox (4+1) com {{ local.hero_product_name }}. Ideal para {{ local.persona }}."
---

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Service",
  "serviceType": "FoodDelivery",
  "name": "Entrega ChefBox - {{ local.name }}",
  "provider": { "@type": "LocalBusiness", "name": "ChefBrico" },
  "areaServed": {
    "@type": "Place",
    "name": "{{ local.name }}",
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": "{{ local.geo.latitude }}",
      "longitude": "{{ local.geo.longitude }}"
    }
  }
}
</script>

<style>
    /* DESIGN DE REVISTA DIGITAL (WAR-GBP) */
    .geo-hero { background-color: #fdfbf7; padding: 60px 20px; text-align: center; border-bottom: 4px solid #014039; }
    .geo-tag { background: #e8f5e9; color: #2e7d32; padding: 8px 18px; border-radius: 50px; font-weight: bold; text-transform: uppercase; display: inline-block; margin-bottom: 15px; }
    .geo-title { font-family: 'Merriweather', serif; color: #014039; font-size: 2.2rem; margin-bottom: 20px; }
    .geo-pain { font-size: 1.2rem; color: #555; max-width: 700px; margin: 0 auto 30px; font-style: italic; }
    
    .geo-solution-box {
        background: white; border: 2px solid #bfb084; border-radius: 15px; padding: 30px;
        max-width: 600px; margin: 0 auto; box-shadow: 0 10px 30px rgba(0,0,0,0.05); position: relative;
    }
    .promo-badge {
        position: absolute; top: -15px; right: -15px; background: #d35400; color: white;
        padding: 10px 20px; border-radius: 50px; font-weight: bold;
    }
    
    .geo-btn-start-game {
        background-color: #F2811D; color: white; text-decoration: none; padding: 18px 30px;
        border-radius: 50px; font-weight: bold; font-size: 1.2rem; display: inline-flex;
        align-items: center; gap: 10px; margin-top: 25px; width: 100%; justify-content: center;
    }
</style>

<main>
    <section class="geo-hero">
        <div class="container">
            <span class="geo-tag">📍 Logística Confirmada: {{ local.name }}</span>
            <h1 class="geo-title">Mora na {{ local.name }} e quer praticidade?</h1>
            <p class="geo-pain">"{{ local.pain_point }}"</p>

            <div class="geo-solution-box">
                <div class="promo-badge">🔥 OFERTA 4+1</div>
                <h3 style="color: #014039; margin-bottom: 10px;">Recomendação para {{ local.persona }}</h3>
                <p>Para resolver sua rotina na <strong>{{ local.name }}</strong>, a Chef Maria sugere seu favorito:</p>
                <h2 style="font-size: 1.8rem; margin: 15px 0; color: #d35400;">🍲 {{ local.hero_product_name }}</h2>
                
                <ul style="text-align: left; margin: 20px auto; max-width: 300px; color: #555;">
                    <li>✅ Frete Grátis para {{ local.name }}</li>
                    <li>✅ Entrega estimada: {{ local.eta }}</li>
                    <li>✅ Próximo a você: {{ local.nearby | join(", ") }}</li>
                </ul>

                <a href="/chefbox/" class="geo-btn-start-game">MONTAR MINHA CHEFBOX AGORA ➜</a>
            </div>

            <a href="https://www.google.com/maps/dir/?api=1&destination={{ local.geo.latitude }},{{ local.geo.longitude }}" target="_blank" style="display:block; margin-top:30px; color:#777; text-decoration:underline;">
                📍 Ver rota de Vicente Pires até {{ local.name }} no Maps
            </a>
        </div>
    </section>
</main>
