---
layout: base.njk
permalink: /receitas/solucoes/dieta-performance/
title: "Cardápio Dieta e Performance | Curadoria da Chef"
description: "Potencialize seus treinos e foco com a seleção funcional da ChefBrico. Sopas proteicas, detox e low carb 100% naturais."
---

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "CollectionPage",
      "name": "Soluções de Dieta e Performance",
      "description": "Curadoria de pratos funcionais para atletas e vida saudável.",
      "url": "https://gourmetpratico.com.br/receitas/solucoes/dieta-performance/",
      "editor": {
        "@type": "Person",
        "name": "Maria Conceição",
        "jobTitle": "Chef de Criação e Especialista em Cozinha Natural"
      }
    }
  ]
}
</script>

<style>
    .solucao-container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
    
    .solution-header { text-align: center; padding: 60px 20px 40px; }
    .solution-header h1 { font-size: 2.8rem; color: #014039; font-family: 'Merriweather', serif; }
    .solution-header p { color: #555; font-size: 1.1rem; max-width: 650px; margin: 15px auto; }

    /* BOX DE AUTORIDADE DA CHEF */
    .expert-quote-box {
        background: #fdfcf7; border-left: 5px solid #27ae60;
        padding: 30px; border-radius: 12px; margin-bottom: 50px;
        display: flex; align-items: center; gap: 20px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.03);
    }
    .expert-avatar { width: 90px; height: 90px; border-radius: 50%; object-fit: cover; border: 3px solid #fff; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }

    .section-title {
        color: #27ae60; font-size: 1.6rem; margin: 50px 0 25px;
        display: flex; align-items: center; gap: 12px; font-family: 'Merriweather', serif;
    }
    .section-title::before { content: ''; display: block; width: 35px; height: 3px; background: #27ae60; }

    /* GRID DE PERFORMANCE (2x6) */
    .recipe-grid { 
        display: grid; gap: 15px; width: 100%; margin-bottom: 40px;
        grid-template-columns: repeat(2, 1fr); 
    }
    @media (min-width: 1024px) { 
        .recipe-grid { grid-template-columns: repeat(6, 1fr); gap: 20px; } 
    }

    .recipe-card { 
        background: white; border: 1px solid #eee; border-radius: 12px; overflow: hidden; 
        display: flex; flex-direction: column; box-shadow: 0 2px 8px rgba(0,0,0,0.05); 
        height: 100%; transition: 0.2s; text-align: center; 
    }
    .recipe-card:hover { transform: translateY(-3px); border-color: #27ae60; }

    .card-image-wrapper { position: relative; width: 100%; padding-top: 75%; }
    .card-image-wrapper img { position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; }
    .btn-card-float { 
        position: absolute; bottom: 8px; right: 8px; width: 35px; height: 35px; 
        background-color: #27ae60; color: white; border-radius: 50%; border: none; 
        font-size: 1.4rem; display: flex; align-items: center; justify-content: center; 
        cursor: pointer; box-shadow: 0 2px 5px rgba(0,0,0,0.2); 
    }

    .card-content { padding: 12px; flex-grow: 1; display: flex; flex-direction: column; align-items: center; }
    .card-title { font-size: 0.85rem; color: #014039; margin: 5px 0 10px; font-weight: 700; text-decoration: none; line-height: 1.3; }
    .card-price { color: #27ae60; font-weight: 800; font-size: 1rem; }

    /* BANNER CHEFBOX */
    .bundle-banner {
        background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);
        color: white; padding: 50px; border-radius: 15px; text-align: center;
        margin: 80px 0; box-shadow: 0 10px 25px rgba(39, 174, 96, 0.2);
    }
    .btn-white {
        background: white; color: #27ae60; padding: 14px 30px; border-radius: 50px;
        text-decoration: none; font-weight: bold; display: inline-block; margin-top: 20px;
    }
</style>

<main class="solucao-container">
    <header class="solution-header">
        <span style="text-transform:uppercase; letter-spacing:2px; color:#999; font-size:0.75rem; font-weight:bold;">Curadoria ChefBrico</span>
        <h1>Dieta & Performance</h1>
        <p>Combustível limpo para o seu corpo. Sem inflamação, sem peso no estômago, apenas energia real para seus treinos e foco.</p>
    </header>

    <div class="expert-quote-box">
        <img src="/imagens/maria-conceicao-chefbrico.webp" alt="Maria Conceição" class="expert-avatar">
        <div>
            <h4 style="margin:0; color:#014039;">A Curadoria da Chef</h4>
            <p style="margin:5px 0 0; font-style:italic; font-size: 0.95rem;">"Priorizei ingredientes anti-inflamatórios e de fácil digestão. O objetivo aqui é nutrir a célula. É comida para quem precisa de foco e recuperação muscular."</p>
        </div>
    </div>

    <h3 class="section-title">💪 Recuperação & Força (Proteicos)</h3>
    <div class="recipe-grid">
        {% for item in collections.receitas %}
            {% if item.data.solucoes and "Performance" in item.data.solucoes and "Proteico" in item.data.tags %}
            <div class="recipe-card">
                <div class="card-image-wrapper">
                    <a href="{{ item.url }}"><img src="{{ item.data.image }}" alt="{{ item.data.title }}" loading="lazy"></a>
                    <button id="prod-{{ item.data.sku }}" onclick="addToGame('{{ item.data.title }}', {{ item.data.price }}, '{{ item.data.image }}', '{{ item.data.sku }}')" class="btn-card-float">+</button>
                </div>
                <div class="card-content">
                    <a href="{{ item.url }}" class="card-title">{{ item.data.title | replace(" | ChefBrico", "") }}</a>
                    <div style="margin-top: auto;"><span class="card-price">R$ {{ item.data.price | dinheiro }}</span></div>
                </div>
            </div>
            {% endif %}
        {% endfor %}
    </div>

    <h3 class="section-title">🌿 Detox & Leveza (Jantar Leve)</h3>
    <div class="recipe-grid">
        {% for item in collections.receitas %}
            {% if item.data.solucoes and "Performance" in item.data.solucoes and "Detox" in item.data.tags %}
            <div class="recipe-card">
                <div class="card-image-wrapper">
                    <a href="{{ item.url }}"><img src="{{ item.data.image }}" alt="{{ item.data.title }}" loading="lazy"></a>
                    <button id="prod-{{ item.data.sku }}" onclick="addToGame('{{ item.data.title }}', {{ item.data.price }}, '{{ item.data.image }}', '{{ item.data.sku }}')" class="btn-card-float">+</button>
                </div>
                <div class="card-content">
                    <a href="{{ item.url }}" class="card-title">{{ item.data.title | replace(" | ChefBrico", "") }}</a>
                    <div style="margin-top: auto;"><span class="card-price">R$ {{ item.data.price | dinheiro }}</span></div>
                </div>
            </div>
            {% endif %}
        {% endfor %}
    </div>

    <h3 class="section-title">🔥 Foco Low Carb (Baixo IG)</h3>
    <div class="recipe-grid">
        {% for item in collections.receitas %}
            {% if item.data.solucoes and "Performance" in item.data.solucoes and "Low Carb" in item.data.tags %}
            <div class="recipe-card">
                <div class="card-image-wrapper">
                    <a href="{{ item.url }}"><img src="{{ item.data.image }}" alt="{{ item.data.title }}" loading="lazy"></a>
                    <button id="prod-{{ item.data.sku }}" onclick="addToGame('{{ item.data.title }}', {{ item.data.price }}, '{{ item.data.image }}', '{{ item.data.sku }}')" class="btn-card-float">+</button>
                </div>
                <div class="card-content">
                    <a href="{{ item.url }}" class="card-title">{{ item.data.title | replace(" | ChefBrico", "") }}</a>
                    <div style="margin-top: auto;"><span class="card-price">R$ {{ item.data.price | dinheiro }}</span></div>
                </div>
            </div>
            {% endif %}
        {% endfor %}
    </div>

    <div class="bundle-banner">
        <h2>Leve a Rotina Completa!</h2>
        <p>Monte sua ChefBox com 4 desses pratos e ganhe o 5º de presente.</p>
        <a href="/receitas/" class="btn-white">Montar Meu Kit Performance 🎁</a>
    </div>
</main>
