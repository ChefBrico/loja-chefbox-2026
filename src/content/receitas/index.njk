---
layout: base.njk
permalink: /receitas/
title: "Catálogo Inteligente: Encontre sua Solução | ChefBrico"
description: "Explore o cardápio completo da ChefBrico. Filtre por solução, dieta ou tipo de prato."
---

<style>
    /* SEU CSS ORIGINAL MANTIDO */
    .page-header-local { text-align: center; margin: 40px 20px 30px; }
    .page-header-local h1 { font-family: 'Merriweather', serif; color: #2c3e50; margin-bottom: 10px; }
    .page-header-local .subtitle { font-family: 'Lato', sans-serif; color: #7f8c8d; max-width: 700px; margin: 0 auto; }

    .filter-section { margin-bottom: 30px; text-align: center; }
    .filter-title { font-weight: bold; text-transform: uppercase; letter-spacing: 1px; color: #27ae60; margin-bottom: 15px; font-size: 0.9em; }
    
    .filter-buttons { display: flex; flex-wrap: wrap; gap: 10px; justify-content: center; padding-bottom: 20px; border-bottom: 1px solid #eee; margin-bottom: 20px; }
    .filter-btn { background-color: #f9f9f9; color: #555; padding: 8px 20px; border-radius: 20px; border: 1px solid #eee; cursor: pointer; font-size: 0.9em; font-weight: bold; transition: all 0.2s; }
    .filter-btn:hover, .filter-btn.active { background-color: #27ae60; color: white; border-color: #27ae60; }

    .product-grid { 
        display: grid; 
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); 
        gap: 30px; 
        max-width: 1200px; 
        margin: 0 auto; 
        padding-bottom: 150px; /* Espaço para a régua do jogo não cobrir */
    }
    
    .product-card { 
        display: flex; flex-direction: column; 
        border: 1px solid #eee; border-radius: 15px; 
        overflow: hidden; background-color: white; 
        box-shadow: 0 5px 15px rgba(0,0,0,0.03); 
        transition: transform 0.3s; 
        text-decoration: none; color: #333; 
        cursor: pointer;
    }
    .product-card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px rgba(0,0,0,0.1); }
    
    .product-card-image { width: 100%; height: 200px; object-fit: cover; background: #f0f0f0; }
    
    .product-card-content { padding: 20px; flex-grow: 1; display: flex; flex-direction: column; }
    .product-card-content h4 { font-size: 1.2em; margin: 0 0 10px; color: #2c3e50; font-family: 'Merriweather', serif; }
    .product-card-content p { font-size: 0.9em; color: #666; margin-bottom: 20px; flex-grow: 1; }
    
    .card-actions { margin-top: auto; display: flex; gap: 10px; }
    
    .btn-ver { 
        flex: 1; border: 1px solid #ddd; color: #666; padding: 10px; 
        text-align: center; border-radius: 8px; font-weight: bold; font-size: 0.9rem;
    }
    .btn-add { 
        flex: 1; background: #F2811D; color: white; border: none; padding: 10px; 
        text-align: center; border-radius: 8px; font-weight: bold; cursor: pointer;
        box-shadow: 0 4px 0 #c0600c; font-size: 0.9rem;
    }
    .btn-add:active { transform: translateY(2px); box-shadow: none; }
</style>

<main class="container">
    <div class="page-header-local">
        <h1>Nosso Catálogo Inteligente</h1>
        <p class="subtitle">Encontre a solução perfeita para cada momento. Use nossos filtros por Solução ou Categoria para navegar em nosso universo de sabores.</p>
    </div>

    <!-- FILTROS POR SOLUÇÃO -->
    <div class="filter-section">
        <div class="filter-title">🔎 Navegue por Solução</div>
        <div class="filter-buttons">
            <button class="filter-btn active" onclick="filter('all')">Todos</button>
            <button class="filter-btn" onclick="filter('jantar-rapido')">Jantar Rápido</button>
            <button class="filter-btn" onclick="filter('marmita-saudavel')">Marmita Saudável</button>
            <button class="filter-btn" onclick="filter('momento-especial')">Momentos Especiais</button>
            <button class="filter-btn" onclick="filter('kids')">Para a Família</button>
            <button class="filter-btn" onclick="filter('performance')">Performance & Dieta</button>
            <button class="filter-btn" onclick="filter('trilha')">Trilha & Viagem</button>
        </div>
    </div>

    <!-- FILTROS POR CATEGORIA -->
    <div class="filter-section">
        <div class="filter-title">🍲 Navegue por Categoria</div>
        <div class="filter-buttons">
             <button class="filter-btn" onclick="filter('Risoto')">Risotos</button>
             <button class="filter-btn" onclick="filter('Sopa')">Sopas</button>
             <button class="filter-btn" onclick="filter('Refeição')">Refeições Integrais</button>
             <button class="filter-btn" onclick="filter('Empório')">Empório da Chef</button>
        </div>
    </div>
    
    <!-- GRADE DE PRODUTOS -->
    <div class="product-grid">
        
        {% for item in collections.receitas %}
        <!-- O Card lê as tags para o filtro funcionar -->
        <div class="product-card" data-tags="{{ item.data.keywords }} {{ item.data.category }}">
            
            <!-- Link da Imagem -->
            <a href="{{ item.url }}">
                <img src="{{ item.data.image }}" alt="{{ item.data.title }}" class="product-card-image" loading="lazy">
            </a>

            <div class="product-card-content">
                <a href="{{ item.url }}">
                    <h4>{{ item.data.title }}</h4>
                </a>
                <p>{{ item.data.description | truncate(80) }}</p>
                
                <!-- CORREÇÃO 1: Preço com Filtro Dinheiro (R$ 34,80) -->
                <div style="font-weight:bold; color:#27ae60; margin-bottom:10px;">
                    R$ {{ item.data.price | dinheiro }}
                </div>

                <div class="card-actions">
                    <!-- Botão 1: Ver Receita -->
                    <a href="{{ item.url }}" class="btn-ver">Ver Receita</a>
                    
                    <!-- CORREÇÃO 2: Botão Adicionar (Sem aspas no preço) -->
                    <button class="btn-add" onclick="addToGame('{{ item.data.title }}', {{ item.data.price }}, '{{ item.data.image }}')">
                        + CAIXA 🎁
                    </button>
                </div>
            </div>
        </div>
        {% endfor %}

    </div>
</main>

<!-- SCRIPT DE FILTRO INTELIGENTE -->
<script>
function filter(tag) {
    const cards = document.querySelectorAll('.product-card');
    const btns = document.querySelectorAll('.filter-btn');
    
    // Atualiza visual dos botões
    btns.forEach(b => {
        if(b.innerText.toLowerCase().includes(tag) || b.getAttribute('onclick').includes(tag)) {
            b.classList.add('active');
        } else {
            b.classList.remove('active');
        }
    });

    // Filtra os cards
    cards.forEach(card => {
        const tagsDoProduto = card.getAttribute('data-tags').toLowerCase();
        const busca = tag.toLowerCase();

        if (tag === 'all' || tagsDoProduto.includes(busca)) {
            card.style.display = 'flex';
        } else {
            card.style.display = 'none';
        }
    });
}
</script>
