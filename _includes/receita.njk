---
layout: base.njk
---

<article class="receita-container">
    
    <!-- 1. CABEÇALHO (Mantido limpo para SEO) -->
    <header class="recipe-header text-center">
        <span class="badge-categoria">{{ category }}</span>
        <h1 class="recipe-title">{{ title }}</h1>
        <p class="recipe-subtitle">{{ description }}</p>
        
        <div class="recipe-meta-icons">
            <span>⏱ {{ tempo_preparo }}</span>
            <span>👥 {{ rendimento }}</span>
            <span>🔥 {{ dificuldade }}</span>
        </div>

        <!-- === NOVO: BOTÃO DE AÇÃO A-COMMERCE (CONECTADO AO JOGO) === -->
        <!-- Este botão garante que sempre haja como comprar, mesmo sem ler o texto -->
        <div class="recipe-cta-block" style="margin-top: 25px; text-align: center;">
            <div style="font-size: 1.5rem; font-weight: bold; color: var(--cor-cta); margin-bottom: 10px;">
                R$ {{ price }}
            </div>
            
            <button 
                onclick="addToGame('{{ title }}', '{{ price }}', '{{ image }}')" 
                class="btn-comprar-agora" 
                style="cursor: pointer; border: none; font-size: 1rem; padding: 15px 30px; background: var(--cor-cta); color: white; border-radius: 50px; font-weight: bold; box-shadow: 0 4px 15px rgba(242, 129, 29, 0.4);">
                ADICIONAR À CHEFBOX (+1) 🎁
            </button>
            
            <p style="font-size: 0.8rem; color: #666; margin-top: 8px;">
                Junte 5 sabores e pague apenas 4!
            </p>
        </div>
        <!-- ========================================================== -->

    </header>

    <!-- 2. CONTEÚDO RICO (Texto, Fotos, História) -->
    <div class="recipe-content-body">
        {{ content | safe }}
    </div>

    <!-- 3. FAQ AUTOMÁTICO -->
    {% if faq and not content.includes('faq-section') %}
    <section class="faq-section">
        <h3>Dúvidas Frequentes</h3>
        <div class="faq-grid">
            {% for item in faq %}
            <details class="faq-item">
                <summary>{{ item.question }}</summary>
                <p>{{ item.answer }}</p>
            </details>
            {% endfor %}
        </div>
    </section>
    {% endif %}

    <!-- 4. RODAPÉ DE AUTORIDADE -->
    <div class="author-bio-box">
        <img src="/imagens/maria-conceicao.jpg" alt="Maria Conceição" class="author-avatar">
        <div>
            <h4>Sobre a Chef Maria Conceição</h4>
            <p>Vencedora do Prêmio Sebrae Mulher de Negócios. Criadora do método que une sabor, saúde e praticidade.</p>
        </div>
    </div>

</article>

<!-- SCHEMA.ORG (DADOS PARA O ROBÔ) -->
<script type="application/ld+json">
{
  "@context": "https://schema.org/",
  "@graph": [
    {
      "@type": "Recipe",
      "name": "{{ title }}",
      "image": ["{{ meta.url }}{{ image }}"],
      "author": { "@type": "Person", "name": "{{ author_name }}" },
      "description": "{{ description }}",
      "prepTime": "PT5M",
      "cookTime": "{{ tempo_iso }}",
      "totalTime": "{{ tempo_iso }}",
      "recipeYield": "{{ rendimento }}",
      "recipeCategory": "{{ category }}"
    },
    {
      "@type": "Product",
      "name": "Kit ChefBox: {{ title }}",
      "sku": "{{ sku }}",
      "gtin": "{{ gtin }}",
      "image": "{{ meta.url }}{{ image }}",
      "brand": { "@type": "Brand", "name": "ChefBrico" },
      "offers": {
        "@type": "Offer",
        "price": "{{ price }}",
        "priceCurrency": "{{ price_currency }}",
        "availability": "{{ availability }}",
        "url": "{{ meta.url }}{{ page.url }}"
      }
    }
  ]
}
</script>
