<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- 1. SEO E METADADOS -->
    <title>{{ title }} | ChefBrico</title>
    <meta name="description" content="{{ description }}">
    <meta name="author" content="Maria Conceição">
    
    <!-- 2. SEO TÉCNICO -->
    <link rel="canonical" href="https://gourmetpratico.com.br{{ page.url }}">

    <!-- 3. REDES SOCIAIS (Open Graph) -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://gourmetpratico.com.br{{ page.url }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ description }}">
    {% if image %}
    <meta property="og:image" content="https://gourmetpratico.com.br{{ image }}">
    {% else %}
    <meta property="og:image" content="https://gourmetpratico.com.br/imagens/logo.png">
    {% endif %}

    <!-- 4. A ROUPA DO SITE (CSS) -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- 5. O PORTEIRO DO CMS -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    <!-- 6. SCHEMA.ORG -->
    {% if schema %}
    <script type="application/ld+json">
        {{ schema | safe }}
    </script>
    {% endif %}
</head>
<body>
    
    <!-- A. CABEÇALHO -->
    {% include "header.njk" %}
    
    <!-- B. CONTEÚDO PRINCIPAL -->
    <main>
        {{ content | safe }}
    </main>
    
    <!-- C. RODAPÉ -->
    {% include "footer.njk" %}
    
    <!-- D. A-COMMERCE: RÉGUA DE GAMIFICAÇÃO -->
    <!-- Lógica: Aparece APENAS em Receitas e ChefBox -->
    {% if '/receitas/' in page.url or '/chefbox/' in page.url %}
        {% include "gamification-bar.njk" %}
    {% endif %}
    
    <!-- E. A-COMMERCE: MODAL DE CHECKOUT (GLOBAL) -->
    <!-- A janela que pede o endereço -->
    {% include "checkout-modal.njk" %}
    
    <!-- F. SCRIPTS -->
    <script src="/js/script.js"></script>
    
    <!-- Script de Login do Admin (Netlify) -->
    <script>
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on("init", user => {
          if (!user) {
            window.netlifyIdentity.on("login", () => {
              document.location.href = "/admin/";
            });
          }
        });
      }
  <script>
    // Força a régua a aparecer se o JS falhar
    window.onload = function() {
        document.querySelector('.chefbox-sticky-bar').style.display = 'flex';
    }
</script>

<!-- === COMPONENTE: BARRA DE JOGO (GAMIFICATION) === -->
<div id="chefbox-bar" class="chefbox-sticky-bar">
    <div class="bar-info">
        <span id="game-status-text">Monte sua ChefBox:</span>
    </div>
    
    <div class="slots-wrapper">
        <div id="slot-1" class="slot-circle">1</div>
        <div id="slot-2" class="slot-circle">2</div>
        <div id="slot-3" class="slot-circle">3</div>
        <div id="slot-4" class="slot-circle">4</div>
        <div id="slot-5" class="slot-circle gift">🎁</div>
    </div>

    <button id="btn-finish-game" class="btn-finish-float" onclick="openCheckoutModal()" style="display:none;">
        Finalizar Pedido ➜
    </button>
</div>

<!-- === COMPONENTE: MODAL DE CHECKOUT (ZERO-CLIC) === -->
<div id="checkout-modal" class="modal-overlay" style="display: none;">
    <div class="modal-box">
        <h3 style="color: #014039; text-align: center;">Quase lá! 🛵</h3>
        <p style="text-align: center; margin-bottom: 20px; color: #666;">Confirme seus dados para enviarmos o pedido para a Chef Maria.</p>
        
        <input type="text" id="customer-name" placeholder="Seu Nome" style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
        <input type="text" id="customer-cep" placeholder="Seu CEP" style="width: 100%; padding: 12px; margin-bottom: 10px; border: 1px solid #ddd; border-radius: 8px;">
        <textarea id="customer-address" placeholder="Endereço (Rua, Número, Complemento)" rows="3" style="width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid #ddd; border-radius: 8px;"></textarea>
        
        <button onclick="sendOrderToWhatsApp()" style="width: 100%; padding: 15px; background: #25D366; color: white; border: none; border-radius: 50px; font-weight: bold; font-size: 1.1rem; cursor: pointer;">
            ENVIAR NO WHATSAPP 💬
        </button>
        
        <p onclick="closeCheckoutModal()" style="text-align: center; margin-top: 15px; color: #999; cursor: pointer; text-decoration: underline;">Voltar / Cancelar</p>
    </div>
</div>

<!-- Carrega o Cérebro do Jogo -->
<script src="/js/app.js"></script>
</body>
</html>
